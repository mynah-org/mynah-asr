# Mynah ASR — plan

`ENGINEERING.md` is normative. This file is the **board**: one line per work
item, linking the `.work/` note that holds the detail. Read the note when you
pick the item up; never write detail here. Legend: `[ ]` open · `[~]` in
progress · `[x]` done · `[-]` dropped. `python3 tools/check_plan.py` must pass.

## Mission

A native C runtime for NeMo speech models (Nemotron streaming, Parakeet,
Canary) that is **usable as one concurrent server on a CPU box**: tens of
real-time streams and offline requests per host, several models in one fleet,
no latency spikes, no stalls, every claim provable from the process itself.
**Production is Linux x86-64 and ARM64 and every efficiency decision is taken
there; macOS is the development machine, nice to have, never the target.**

## Current trusted state (2026-09-18, HEAD after `5f0f802`)

- Runtime 0.9.1: six model families, streaming ≡ offline byte-identical,
  oracle parity per stage, int8/int4 (NEON SDOT, AVX-512 VNNI), Metal/CUDA
  opt-in, VAD, LID, word timestamps, GGUF import. Single-stream RTF 0.02–0.15
  on Apple Silicon and EPYC (`docs/benchmarks.md`).
- The server is a **prototype**: blocking pool, one thread per stream, ceiling
  = `--threads`, no timeouts, no metrics, one model per process. **No number
  exists for N concurrent streams on any host.** Audit and design:
  [`.work/serving-v2-design.md`](.work/serving-v2-design.md).
- Method adopted from qwen-tts / mynah-tts on 2026-09-18:
  [`.work/engineering-method.md`](.work/engineering-method.md).

## Work board

### Reference — read before starting anything
- [x] The v2 serving design, what it borrows, what is ASR-specific, and the
      ideas the siblings already falsified → [`.work/serving-v2-design.md`](.work/serving-v2-design.md)
- [x] The method → [`.work/engineering-method.md`](.work/engineering-method.md)
- [x] What the siblings measured as wins and losses, their flag machinery, kernel state per ISA → [`.work/sibling-wins-and-flags.md`](.work/sibling-wins-and-flags.md)
- [x] v1 history (Italian, closed, local only): the old plan and TODO live in the untracked private archive folder under the work notes

### S0 — Baseline (trimmed 2026-09-18: the v1 server is not worth measuring; S1/S2 start now)
- [ ] S0-6 **The box day**: the ordered runbook for the Linux host — dispatch proof, identity gates, the two questions macOS could not ask, the qualification, multi-model → [`.work/box-day-plan.md`](.work/box-day-plan.md)
- [~] S0-1 Build, gates and single-stream step cost (Q=4, int8, T=1..4, pinned, idle box) on the 32-core ARM host → [`.work/axion-first-run.md`](.work/axion-first-run.md)
- [~] S0-2 Streaming load tool v0: N WebSocket clients paced at 1x, stdlib only → [`.work/bench-harness-streaming.md`](.work/bench-harness-streaming.md)
- [-] S0-3 The v1 server under N streams — dropped: it is broken by construction and the number would not size v2 → [`.work/baseline-streaming-concurrency.md`](.work/baseline-streaming-concurrency.md)
- [ ] S0-4 Allocation count per chunk on Linux, measured not assumed (needs no box: any Linux) → [`.work/baseline-streaming-concurrency.md`](.work/baseline-streaming-concurrency.md)
- [~] S0-5 **Linux functional gates on every server change**: the GitHub CI runs on ubuntu x86, ubuntu ARM and macOS including the prefork phases, ASan+UBSan and clang-tidy; and on 2026-09-19 the whole model-free suite plus the batched identity gates ran on the Axion itself under gcc — a second ISA and a second compiler for the determinism, tiling and pool gates. Still owed: masks read back from the kernel on a pinned prefork run → [`.work/axion-first-run.md`](.work/axion-first-run.md)

### S1 — Library seams the server needs → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [x] S1-1 `mynah_asr_stream_reset` + `need_samples`; slots are pooled, not reopened (gate green 2026-09-18)
- [x] S1-2 Deltas carry a real window: `t0` is the previous delta's `t1`, so the deltas PARTITION the stream and a client places text in time without accumulating anything of its own (it was always 0.0, which made `t1` the only usable field); `is_final`/`is_eou` were already there, and an `eou` is now a point (`t0 == t1`) rather than a window starting at 0. The WebSocket `delta` frame carries `t0`/`t1` beside `audio_s`. The chunk-arrival passthrough is NOT a library API: the server stages the chunk, so it owns the arrival — `emit_ctx.arrival` comes from the slot's own arrival record and the callback runs inside that step (`server/sched.c`). A second copy in the library would be a number the library cannot check → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [x] S1-3 Allocation-free chunk: per-stream scratch, incremental detokenisation — 87 -> 0 allocations per chunk on macOS/Accelerate, gated by `make test-stream-allocs`; the Linux count stays S0-4
- [x] S1-4 Batched stream step for B slots, byte-identical to B single steps: `mynah_asr_stream_step_batch` stacks the ready chunks as one `[Σq, d]` pass; int8 exact by construction (native per-row dot extended to any T, the dequant fallback never reached), f32 measured row-stable on Accelerate and OFF on OpenBLAS until its own gate runs; `tests/test_stream_batch.c` (0 of 266,240 floats differ at B=8, both dtypes) + `make test-stream-batch-allocs` (0 allocations/step); M1 dev signal 693.60 -> 227.00 ms per step at B=8
- [x] S1-5 The pool spins before it parks, with the meter: workers watch an atomic generation for `MYNAH_ASR_POOL_SPIN_US` (default 50, `0` = the pure condvar pool and the A/B arm) before sleeping, so the dozens of small GEMMs a stream step issues back to back stop paying a kernel round trip each. M1 dev signal, the sub-2M-MAC shapes: T=4 318 -> 219 us, T=8 355 -> 285 us; the large shapes do not move. `worker_spin_pct`/`caller_spin_pct` in the SIGUSR1 dump say whether the spin caught anything. **The A/B arm earned its keep immediately**: at `SPIN_US=0` the first dispatch DEADLOCKED (workers parked on the job condvar, caller on the completion condvar, 0% cpu) because a worker started from the live generation and could park on a job it was already counted in — invisible at the default spin, which catches the bump inside its window. `tests/test_threads.c` now gates the first dispatch at three spin budgets in pristine children, and the prefork `threadpool_after_fork` contract in a child of a warm pool. The lane redirect is not lifted: this runtime has no second lane to redirect → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [x] S1-6 BLAS leaves the worker: `src/sgemm.c` is ours, deterministic across thread counts and gated (`tests/test_sgemm.c`), behind one seam, and **`BLAS=none` is the Linux default since 2026-09-19**. What unblocked the decision was finding why `own` had lost: the DOT family — every `x @ W^T`, so every f32 linear, the joint head and the attention scores — ran one dot product at a time into ONE accumulator, i.e. FMA-latency bound at 8.7 GF/s on a core that does ~100. Register-tiled (`SG_DOT_MR x SG_DOT_NC_TILE`, a `1 x SG_DOT_NC_WIDE` strip for a gemv) it does 75-80, the panel shapes 48.9 -> 74.8 (`SG_PANEL_NV` derived instead of chosen), the LSTM gemv 8.2 -> 30.2; at 4-16 stacked rows — the streaming shapes — `own` now BEATS Accelerate's AMX. The tile is a schedule, not a formula: byte-identical to the untiled dot, gated for DOT/PANEL/NARROW by `tiling_identity` in `tests/test_sgemm.c`. The same instrument found a second defect: the parallel threshold was compared against total work rather than work PER THREAD, so the sub-2M-MAC shapes — where the streaming attention lives — cost 630 us at 8 threads against 318 at one; now 330. OpenBLAS keeps building as the comparison arm → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [ ] S1-6a Linux only, now a VERIFICATION rather than an open question, and two commands rather than a box-day: `MYNAH_ASR_GEMM_PROFILE=1` dumps the shapes and call counts the model really issued, `tests/bench_gemm_shapes.c` replays them against both arms interleaved in ONE process and weights each shape by its count. Run it on the Axion and on x86 with `make BLAS=openblas`; if OpenBLAS wins by enough to cost capacity the Makefile default goes back and the note says why. Also owed there: thread count inside a pinned prefork worker (2T vs T) and the OpenBLAS f32 row-stability gate that decides `MYNAH_ASR_BATCH_F32` → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [x] S1-7 Share the rel-pos K-projection across the streams of one batched pass that have the same `K`: computed once per (layer, K-group) into the batch scratch, bit-exact by construction and gated as such (0 of 266,240 floats differ at B=8, int8 and f32, with SHARED/PRIVATE/GROUP counters proving it happened); the single path is untouched and one allocation-free step is unchanged. M1 dev signal UNDER THIRD-PARTY LOAD, best-of-4 interleaved: slope `b` 22.7 -> 20.3 ms (-10.7%), B_max ~8.2 -> ~9.2 — a third of the 7.4 ms the isolated micro-benchmark had promised, discrepancy not explained. Only the largest K-group of a pass is shared; Linux unmeasured → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [~] S1-8 The S1-4 gates on Linux: **done for `own`** on the Axion (Neoverse-V2) — `tests/test_stream_batch.c` on the converted Nemotron pack is IDENTICAL OK at B=1,2,3,4,8 for int8 AND f32, the f32 encoder bit-exact at B=8 (266,240 floats compared, 0 differ, 0/24 caches differ), with the counters proving the fast path ran (208,896 `neon-smmla` kernel blocks, rel-pos shared 2496 / private 0). It also reported the pre-existing es-ES single-vs-offline int8 difference and attributed it correctly instead of letting it discredit the batching gate. STILL OPEN: the OpenBLAS arm (`MYNAH_ASR_BATCH_F32=1` with `make BLAS=openblas`, which decides that default there — the package is not installed on the box) and the step table → [`.work/axion-session-2026-09-19.md`](.work/axion-session-2026-09-19.md)
- [ ] S1-9 The es-ES clip transcribes differently offline and single-stream at int8, on both hosts, and nobody has decided whether that is a quantisation edge or a defect. The batching gates are unaffected and say so; this is the separate question → [`.work/axion-session-2026-09-19.md`](.work/axion-session-2026-09-19.md)

### S2 — The server
- [~] S2-1 Prefork parent, core-major pinned workers, SCM_RIGHTS handoff, `--prefork-plan` (landed; Linux pinned run pending) → [`.work/server-prefork.md`](.work/server-prefork.md)
- [~] S2-2 One scheduler thread per worker owns the model; slots; ingest threads; one path for WS and REST (landed and gated on the M1 dev host: identity at 4 and at 8 streams and under `--prefork`, ubsan clean, the step is one batched call; TSan and the Linux cadence numbers are what is left) → [`.work/server-scheduler.md`](.work/server-scheduler.md)
- [x] S2-2b The scheduler's step is ONE `mynah_asr_stream_step_batch` over the ready set, not one feed per slot: cancellation, fairness, per-slot lag and the finalize path unchanged, scratch reserved at start-up, `batched_steps_total`/`rows_stacked_total`/ready-set/`step_wall_ms` in `/v1/health` and `/metrics`. Quiet-host M1 pair, same gate back to back: 4 streams lag p50/p95 **1219/2193 -> 145/253 ms** (NOT STREAMABLE -> GOOD), WAVE 8x2 **1104/1952 -> 143/258 ms**; new `batched-identity` phase (8 streams, 5 clips, `--cap 8`) byte-identical and proving the stacked path ran → [`.work/server-scheduler.md`](.work/server-scheduler.md)
- [~] S2-3 Admission ladder with per-reason statuses, timeouts, pings, lingering close, graceful shutdown (landed and gated on the M1: every worker refusal goes through the lingering close, `--ping-ms`/`--max-audio-seconds`/idle caps announced with a code, SIGTERM drains with `shutting_down`, exit 0, `leaks --atExit` 0 leaks; the "lag does not move under a cap+1 refusal" line is served by the stalled-reader phase of the stream gate, not measured directly; rung 4 still not enforced at a step boundary; no Linux run) → [`.work/server-admission.md`](.work/server-admission.md)
- [x] S2-4 Asynchronous bounded output writer; backpressure is cancel (module landed with a model-free self-test, TSan and leaks clean; the scheduler writes every frame through it) → [`.work/server-stream-out.md`](.work/server-stream-out.md)
- [~] S2-5 WebSocket protocol v2: control messages, `seq`/`lag_ms`, refusals before the upgrade (query validated before the 101, `unknown_control` and `language_not_served` survive the session, `format=f32le`, `rate=` refused rather than resampled per frame, three utterances on one socket byte-identical to the CLI in `tests/test_server_protocol.sh`; the v1 aliases on `delta`/`done` and `t0`/`t1` are the last step) → [`.work/ws-protocol-v2.md`](.work/ws-protocol-v2.md)
- [~] S2-6 Several models in one fleet: `--model name=dir[:workers=][:cpus=][:cap=][:quant=][:lookahead=]` + `--default`, router by model (WS `?model=`, REST query or multipart field, inside the existing bounded `MSG_PEEK`), `model_not_found` 404 with the accepted set, per-group admission, `/v1/models` from the actual table, `group`/`groups` in `/v1/health`, a `model` label on the router's per-worker series (landed and gated on the M1: `tests/test_server_models.sh` runs ONE server with two groups under `--prefork 2` and proves each group's REST answer byte-identical to THAT model's CLI, the WS identity on the streaming group, 400 `model_not_streaming` on the offline one, 404 on an unknown name, capacity refused per group while the other still serves, survivors=0. NOT done: no Linux/pinned run, so `:cpus=` is a printed plan and not measured isolation; no Canary/AED group and no `--lid-model` in a multi-group fleet; no multi-group SOAK) → [`.work/multi-model-serving.md`](.work/multi-model-serving.md)
- [x] S2-7 Byte-identity gate: N concurrent streams == the same streams alone, also under `--prefork` (`tests/test_server_stream.sh`: 4 real-time streams x 2 utterances and `--prefork 2 --cap 2`, each text byte-identical to `mynah-asr transcribe` of the same clip; `tests/test_server_protocol.sh` adds three utterances on ONE socket) → [`.work/server-scheduler.md`](.work/server-scheduler.md)

### S3 — The server proves what it runs → [`.work/observability.md`](.work/observability.md)
- [x] S3-1 Flag registry + `[FLAGS]`/`[EFFECTIVE-CONFIG]` in library, CLI and server, `[SERVER-CONFIG] v=1` and a per-worker `[TOPOLOGY] v=1` with the mask read back from the kernel
- [x] S3-2 `--dispatch-map [--json]` resolved from owner predicates, `IDLE HARDWARE` footer, ISA guard first in both `main()`s; `mynah-asr-server --dispatch-map` prints the same table
- [x] S3-3 `/v1/health` as facts (cancels by reason, lag p50/p95, resolved kernels, actual mask); `/metrics` on its own port, token bucket, per-worker series, exact threshold counters, the router answering for the fleet
- [x] S3-5 `MYNAH_ASR_GEMM_PROFILE=1`: every call through the f32 seam recorded per distinct shape and dumped at exit (`[GEMM-PROFILE]`/`[GEMM-SHAPE]`), so "which shapes does this model issue, and how often" is a fact per family, per lookahead, per quantisation instead of a reading of the source → [`tests/bench_gemm_shapes.c`](tests/bench_gemm_shapes.c)
- [x] S3-4 `SIGUSR1` one-shot `[DUMP]` bracketed per worker, forwarded by the parent, handler installed before the fork; thread names verified and documented

### S8 — Predict and advise, not just measure → [`.work/where-to-attack.md`](.work/where-to-attack.md)
- [x] S8-0 The serving default is `int8`, not f32: measured 2.24x on one stream (M1, f32 RTF 1.528 vs int8 0.682 — f32 did not sustain ONE real-time stream), against a documented mean CER 0.133 -> 0.145 over 34 locales. Every capacity number in this repo was taken at int8, so the shipped default disagreed with the measurements. The CLI keeps f32, where it is the reference the other modes are checked against
- [x] S8-1 **Calibrate, do not assume**: `tools/bench/box_advisor.py` runs the REAL step table (`tests/test_stream_batch.c`, `MYNAH_ASR_STEP_TIME=1`) on the machine in front of it and fits `a` and `b`, discarding B=1 as warm-up. The sibling TTS doctor has to calibrate a proxy kernel because its frame cost is a composition; this runtime does not, so the coefficients are a fit of the SERVING PATH itself. `--no-calibrate` carries a constant in from another box and the tool then REFUSES to recommend when the two machines differ in cpu count by more than 1.5x — both `a` and `b` are functions of how many cores walk the weights, and carrying the pair across a 4x difference describes neither machine. Original wording:: the advisor runs the step table at B=1,2,4,8 on the actual box and fits `a` and `b` (discarding B=1 as warm-up), because a prediction from a spec sheet is a horoscope. ~30 s
- [x] S8-2 Predict per lookahead with the arithmetic shown and every figure labelled `[MEASURED]`/`[CALIBRATED]`/`[EXTRAPOLATED]`/`[PREDICTED]`/`[UNKNOWN]` — the fallback ladder degrades the LABEL, not the number, and `predict()` returns None rather than inventing a coefficient. Anything past B=8 is marked EXTRAPOLATED because that is where the step table ends. Per-quant is still open. Original wording: with the arithmetic shown and labelled a prediction: `B_max = (rho*P - a) / b` per worker, `W * B_max` for the fleet. Lookahead is the biggest single lever — 3 -> 6 nearly doubles capacity for 240 ms of latency — and belongs in the output as an explicit trade
- [x] S8-3 A ready-to-paste command line with W x T (wide first, and the reason printed), `--cap` at 90% of the prediction so the ladder refuses before the cadence does, `--quant int8`, and the lookahead that meets a stated `--latency-budget-ms`. It refuses on an UNKNOWN dispatch row, on a loaded box, and when the only cost model available came from a different-sized machine. Original wording:: W x T (starting WIDE, see S8-5), `--cap`, `--quant`, the lookahead that meets a stated latency budget, and the backlog/descriptor limits S7 already derives. Refuse when an input is missing rather than substituting a default
- [x] S8-4 Close the loop: **predicted 45, measured 16** on 24 Neoverse-V2 cores (2026-09-20), and the gap is now attributed rather than noted. `mynah_asr_step_b_wall_ms_sum`/`_count` fit `T_step(B)` from the step AS THE SCHEDULER CALLS IT: `a = 39.5 ms, b = 8.57 ms` at c=16 against the bench's `30.8 / 4.58`, and `b` degrades to 10.13 at c=24. Feeding those back gives `B_max = 25` against a ceiling of 16 with 20 breaking — prediction and measurement in the same neighbourhood for the first time. The bench `b` is optimistic by 1.8x because the bench step does not share the machine with ingest, writers, delta JSON and sockets → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md) §7
- [x] S8-5 Both predictions tested on 2026-09-20 and the answers went opposite ways. (a) **`a` is NOT weight traffic**: it went 84.1 → 30.8 ms from 8 to 24 cores, which is compute scaling, not a memory wall — so A5 (bf16, weight layout, prefetch) loses its main justification and must be re-argued before anyone spends a week on it. (b) **FEW WIDE workers is right**, but only after the gate was fixed: as first scored, 8x3 looked twice as good as 1x24, and every winning rung was losing streams that the harness called MARGINAL. Re-scored with losses counted, 1x24 holds c=16 with zero losses and the narrow shapes hold c=8. A third prediction fell out of the same run and is more useful than either: **widening the batch does not move the ceiling** (mean ready set 3.69 → 7.98 for no gain), so the FIXED cost is not the limit — the per-stream term is → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md) §6, §9

### S6 — The fleet answers for itself → [`.work/fleet-observability.md`](.work/fleet-observability.md)
- [ ] S6-1 Fleet metrics through a shared slot table: one `mmap(MAP_SHARED)` before the fork, one fixed slot of atomics per worker, single writer, relaxed adds; the router renders every worker's SCHEDULER counters with a `worker` label beside the routing ones it already has. One scrape target, per-worker series preserved, nothing summed, no IPC in the step (the qwen-tts `qwen_metrics_slot_t` mechanism). Today the router exports routing facts only and the real counters are reachable only by giving each worker its own `--metrics-port`
- [ ] S6-2 Fleet `/v1/health` on the router, and `worker`/`counters_scope` disclosure on the worker's own: a probe currently lands on whichever worker the router picked, so it is a per-worker health wearing the server's name. Readiness and liveness stay ONE endpoint — only a server that HAS a scheduler can be unavailable for want of one
- [ ] S6-3 `configs/observability/`: `prometheus.yml`, `grafana-dashboard.json`, `alerts.yml`, README, and an `observability_up.sh` that provisions both on loopback. Panels translate one-for-one from the sibling once the vocabulary is ASR's; every alert fires on something a LISTENER would notice and every threshold is an exact count, never an estimated quantile
- [ ] S6-4 **Backlog**, the one counter this server is missing: audio received and not yet encoded, as exact threshold counters (`backlog_over_chunks_total`), not a gauge. It is the ASR mirror of the sibling's behind-realtime gap and the one server-side number that says a worker is falling behind before a client notices
- [~] S6-5 Quality, as opposed to speed: **landed** — every utterance gets a CER against the bank's human reference (`--transcripts`), the normaliser is defined once in the metrics module because a CER computed with a different one is a different number wearing the same name, and a run whose cadence held while the transcripts got worse is now **DEGRADED** rather than GOOD. TTFB (first frame of any kind) is reported beside TTFP (first word): together they say whether a wait is transport or model. Known-answer tests pin the normaliser, the edit distance, the rate and the verdict. STILL OPEN: a CER baseline (the `cer_p95` threshold is a placeholder guarding against collapse, not a quality target), and the CER drift track across commits. The load harness must REFUSE to print a KPI table when the batched-identity comparison fails (the sibling's `serve_identity_gate.py` rule — a number from a run whose transcripts moved is worse than no number); a CER drift track against the committed `samples/` bank; and named drift thresholds in the soak the way the sibling names its 30%/20%/resource-growth gates
- [ ] S6-6 A metrics-page watcher under `tools/` (not written yet): scrape the page under load and assert monotonic (no `_total` went backwards), conserved (`completed <= assigned`), balanced (every live worker received something). Model-free, and the only thing that exercises the metrics port under load

### S7 — C100 preconditions (code, before the box) → [`.work/fleet-observability.md`](.work/fleet-observability.md)
- [x] S7-1 The listen backlog is derived from the capacity the ladder already declares (`W x (slots + queue)`, doubled because the connections that will be REFUSED arrive in the same burst as the ones that will be served, floored at `SOMAXCONN`, capped at 4096, overridable with `MYNAH_ASR_LISTEN_BACKLOG`) and printed on `[SERVER-CONFIG]` beside the kernel `somaxconn`, with a WARNING when the kernel clamps it. The defect was reproduced before the fix: `listen(fd,64)` + 150 simultaneous connects completed 64; with the derived backlog it completes as many as `somaxconn` allows
- [x] S7-2 `RLIMIT_NOFILE` is raised toward the hard limit at start-up with a halving backoff and REPORTED AS IT BECAME, not as it was asked for; a loud WARNING names soft, hard and the need when the ceiling is below the configuration, because a stream refused by `accept()` with EMFILE is a refusal no rung of the ladder issued and no counter explains
- [ ] S7-3 The load generator is one PROCESS per stream, so at C100 it competes with the server for cores. The sibling MEASURED this on the same Axion: an unpinned generator cost worker 0 ~8% of first-audio latency. Either a second host generates the load, or the generator is pinned to a reserved slice excluded from the server's mask — and the harness records which, because a C100 number taken with the generator on the server's cores is not a C100 number

### S4 — Measure, then qualify → [`.work/bench-harness-streaming.md`](.work/bench-harness-streaming.md)
- [x] S4-1 Streaming metrics module: every metric defined once, known-answer self-test in `make test` ([`tools/bench/streaming_metrics.py`](tools/bench/streaming_metrics.py))
- [x] S4-2 WAVE / SOAK modes, mixed-length bank, manifest, drift gate, refusal on pacing failure ([`tools/bench/stream_load.py`](tools/bench/stream_load.py); exercised on the macOS dev host only — the qualifying run is S4-3)
- [ ] S4-3 First SOAK on the ARM box at the derived operating point (c=16, 1x24): the ladder screened it on 2026-09-20 and screening never promotes. It is what moves `configs/perf/axion-c4a-highcpu32-nemotron-streaming.json` from `screened` to `qualified` → [`.work/axion-first-run.md`](.work/axion-first-run.md)
- [x] S4-5 [`tools/bench/box_session.sh`](tools/bench/box_session.sh): doctor → dispatch map → calibration → WAVE ladder → SOAK → REST ladder as ONE budgeted command. It polls `/v1/health` and refuses with the server's own log when the process is not serving — the first Axion ladder ran five rungs against a server that had exited on an unsupported `--lookahead` flag and produced five well-formed artefacts containing nothing (`.work/axion-session-2026-09-20.md` §1). `sleep 25` is not a readiness check
- [x] S4-6 [`tools/bench/rest_load.py`](tools/bench/rest_load.py): the concurrency ladder for models the runtime refuses to stream. Reports xRT (throughput) beside per-request latency and names the knee without choosing it; a 503 is counted as admission working, a non-503 invalidates the rung
- [x] S4-8 [`configs/perf/`](configs/perf/README.md) + [`tools/perf_profile.py`](tools/perf_profile.py): one JSON per box shape and model — topology, settings, the dispatch rows the binary MUST resolve, the gates, and what was measured with its date. `check` is a preflight that refuses when the world disagrees; `validate` refuses a profile that claims `qualified` with no soak behind it; every environment variable carries a `value` and a `why`, and `value: null` means it must be ABSENT. Written because three runs on 2026-09-20 were invalid in ways their numbers did not show, and a configuration that lives in shell history cannot be compared with itself a week later
- [x] S4-9 A lost stream disqualifies a rung. `reader: timed out` was scored MARGINAL beside the percentiles; it is a session the caller lost, and it made a W x T sweep read backwards. A 503 is deliberately still MARGINAL: refused at the door and dropped mid-sentence are different outcomes
- [ ] S4-7 Run the stress bank (`make fetch-stress-bank`) and soak against it instead of the five committed clips: the bank manifest has never been produced, so every soak so far has had a four-clip corpus
- [x] S4-4 Serving operations doc: how a new box is approached, the vocabulary, the envelope → [`docs/serving.md`](docs/serving.md)

### S5 — Kernels for the batched step, ARM and x86 in one step → [`.work/cpu-kernels-arm-x86.md`](.work/cpu-kernels-arm-x86.md)
- [x] S5-1 Weight-stationary int8 GEMM for T <= 64, bit-exact vs single rows: SMMLA (2x2 tile), SDOT x4, EVEX and VEX VPDPBUSD, AVX2 — all behind target attributes in every build, chosen by a runtime probe, opt-out through `MYNAH_ASR_CAPS`. `--dispatch-map` splits `kernel.int8_dot` (one row) from `kernel.int8_rows` (the batched pass). It caught a real defect on the way (the `-ffast-math` epilogue grouping `s*ws*sx` differently at two inline sites, now pinned by a value barrier). **CLOSED 2026-09-19 on the Axion (Neoverse-V2, i8mm present)**: `simd=neon+dotprod+i8mm`, `kernel.int8_rows` resolves to `neon-smmla`, and `tests/test_qmat.c` reports `neon-smmla exact OK, 82 shape/T cases, 466 kernel blocks` — the kernel EXECUTES and is bit-exact, which is what `[~]` was waiting for. The x86 arms are still cross-compiled only and skip with a reason → [`.work/cpu-kernels-arm-x86.md`](.work/cpu-kernels-arm-x86.md)
- [ ] S5-2 Depthwise conv with transposed weights and SIMD FMA, gated by the profile
- [ ] S5-3 KleidiAI as an A/B arm behind `KLEIDI=1`
- [ ] S5-4 `SIMD=` profiles with the double test, build-flag stamp, link-only CI per ISA
- [ ] S5-5 Pool-meter run on the batched step; choose W×T from the break-even rule

### Hygiene → [`.work/repo-hygiene.md`](.work/repo-hygiene.md)
- [x] H-1 the per-language samples manifest under the test audio is generated by `make fetch-lang-samples`: say so or track it
- [x] H-2 Stale test-file reference in `tools/gen_kquant_ref.py`
- [x] H-3 `make check` (plan + repo + flag registry) green locally and a CI step (runs on the next push)

### Multi-model — the fleet beyond Nemotron → [`.work/multi-model-streaming.md`](.work/multi-model-streaming.md)
- [x] M-1 A model that declares streaming presets but uses something the incremental encoder does not implement (linear biases, a folded batch_norm, xscaling, symmetric conv padding, per-feature mel normalisation, a subsampling factor other than 8) is REFUSED with a named reason instead of emitting plausible wrong text: `mynah_asr_stream_unsupported` asks the loaded weights, the server asks it before the WebSocket upgrade (`400 model_not_streaming`, the reason in the body). No shipped pack trips it — that is the point
- [x] M-2 The model constants that were not: the subsampling factor is now DERIVED from the convolutions `src/subsampling.c` applies (`MYNAH_ASR_SS_STAGES`/`MYNAH_ASR_SS_FACTOR`) and a pack whose `encoder.subsampling_factor` disagrees is REFUSED at load rather than silently mis-timestamping every word; the CTC blank comes from `decoder.blank_id` (a pure-CTC pack declares it, a hybrid's auxiliary head keeps the last index because the pack's `blank_id` belongs to its RNNT/TDT decoder); the VAD reads its own `sample_rate`; and the server asks `mynah_asr_sample_rate()` instead of dividing by 16000 in six places — the `rate=` refusal now names the model's rate. No behaviour changes on any shipped pack (the converter writes `blank_id = vocab_size - 1`, every pack is 16 kHz and subsamples by 8): the point is that each of these was true by coincidence
- [ ] M-3 Converter builder for `nemotron-speech-streaming-en-0.6b` (cache-aware, left context 70): the only cache-aware model not served, and for a converter reason rather than an architectural one
- [ ] M-4 Buffered streaming for Parakeet behind an explicit stream mode, in its own group. **Confirmed at runtime, not only by arithmetic**: `mynah_asr_stream_unsupported()` refuses every converted Parakeet pack on three counts (linear biases, folded batch_norm, symmetric conv padding), so the server declines the upgrade with a name and Parakeet has no streaming concurrency to measure — only offline throughput (S4-5). Buffering costs ~7x real time per stream against ~1.05x for cache-aware Nemotron; the cap comes from a measured step, and `docs/serving.md` states the multiplier before a line is written
- [x] M-6 **Parakeet fast characterisation on Axion, DONE 2026-09-23 — SCREEN ONLY, promotes nothing.** Same 498-clip bank, same session, same box and the SAME scorers as the C=80 qualification (`wer`/`cer`/`wer_format_free`/`normalise` imported from `streaming_metrics`, never reimplemented), thread invariance gated at 8 clips per model with 0 mismatches. **Parakeet TDT 110m (Q4_K_M) beats Nemotron 0.6b (int8) on this English corpus while being 5.2x faster**: WER mean 0.0916 against 0.1045 offline and 0.1063 streaming, format-free 0.0641 against 0.0759, 0 errors and 0 empty transcripts on both, 26.3 s against 135.9 s of wall for 6585 s of audio. Streaming costs Nemotron +0.0018 WER against its own offline path. Offline capacity: xRT 40 at C=1 (283 ms p50, 3.78 of 5 threads) saturating between C=4 and C=8 (198 xRT) and only +9.7% more by C=32 while median latency goes 614 -> 2529 ms; **0 errors and 0 refusals at every rung up to C=32, so the knee is not a limit**; at C=64 the fleet is at capacity (426/512 served) and the remaining 86 split between clean 503s and client-side broken pipes -- tripling the generator's cpus AT THE SERVER'S EXPENSE moved that split from 34/52 to 51/30 while served requests barely changed (426 -> 431), so the admission ladder refuses correctly and a starved generator was breaking the connection before receiving its own 503. C=64 and C=128 stay INVALID rungs and 30 errors at 6 generator cpus remain unexplained. Cores plateau at 18.7-19.8 of 30 from C=8 up — the same shape as S12-7c on a different model, decoder family and code path, which makes a Nemotron-specific explanation less likely without identifying the cause. **Not comparable as serving semantics**: Parakeet has no streaming mode and the runtime refuses to pretend (M-1/M-4) -> [`.work/parakeet-screen-axion-2026-09-23.md`](.work/parakeet-screen-axion-2026-09-23.md)
- [-] M-5 Canary streaming: out of v2 by design — its AED decoder keeps no state between calls, so it re-decodes the window on top of the 7x encoder

### S9 — The encoder path under the scheduler → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md)
- [x] S9-1 A group of one goes through the stacked path. `B == 1` returned to the single path "verbatim" and `if (batched && g > 1)` sent a lone preset group there too, so a chunk ran as a chain of `[1, d]` GEMVs instead of one `[R, d]` GEMM: **86.7 ms against 19.2 ms per stream**, and two streams together cost less than one alone. The step table called that row warm-up for days; it was not. Rule 4 holds (IDENTICAL, `dot_rows 0 → 3840`), and the identity gate — which compared B=1 against the single path while B=1 WAS the single path — stops being vacuous
- [ ] S9-2 Measure S9-1 on the box: step table and the same ladder as the 2026-09-20 baseline (c=16 held, c=20 broke 3/3). The prediction is that low concurrency improves most and the ceiling moves some; if the ceiling does not move at all, the per-stream term is elsewhere
- [ ] S9-3 Why does `b` grow with concurrency (8.57 at c=16, 10.13 at c=24)? A per-stream cost that degrades under load is the thing that actually sets this ceiling, and nothing in the cost model predicts it. Candidates, in order: cache pressure from B live KV caches, the decoder's f32 pred-net per emitted token (A7), and the per-delta JSON on the scheduler thread
- [ ] S9-4 The stall: every stream gets a first partial, then nothing for tens of seconds, then partial recovery. Seen twice on used servers, never on five clean repeats, and **every prefork topology in the W x T sweep lost streams to it while 1x24 lost none**. `box_session.sh` now samples `steps_total` through a rung, so the next occurrence says whether the server stopped stepping
- [ ] S9-5 Take the offline REST work and the finalize tails off the scheduler thread, or ration them with a wall-clock budget per step: `sched_run_jobs()` stalls every stream on the worker for its duration and the code says so in a comment

### S10 — After the audit of 2026-09-20: the hot path beyond F27, the harness, quality, the tiers → [`.work/serving-audit-metrics-tests-hotpath.md`](.work/serving-audit-metrics-tests-hotpath.md)
- [x] S10-1 One rel-pos table per layer at load, `RK = pe(Kmax) @ relk_w^T`, indexed by `(Kmax-K)` and sized K+Q-1 rows (half of every projected row is never read by the rel_shift). Removes `relpos_priv`, `relpos`, `pos_emb` per step and the S1-7 group machinery; bit-identical by the DOT-family row independence, gated by `tests/test_stream_batch.c`; **LANDED 2026-09-21** (`5fc9d36`): 0 of 266,240 floats differ at B=8, int8 and f32, with the table built BETWEEN the single and batched passes so the comparison is table-vs-per-step; the position embedding is no longer built at all; `MYNAH_ASR_RELPOS_TABLE=0` restores S1-7 and is equally exact. Dev host `b` 18.52 → 14.06 ms/stream (1.32x, Darwin, DIAGNOSTIC). The knee HAS now been remeasured on the Axion 2026-09-21 → **F29**: emission lag p95 at C=40 616 → 229 → 95 ms across arms A/B/C, first breaking concurrency 40 → 70, knee 65 confirmed, `T(B) = 4.02 + 11.84·B` (R²=1.00000) over B 1.36–17.04 against the frozen `14.52 + 19.00·B`
- [x] S10-2 RNNT joint head off the small-T serial dot path: `mynah_asr_qmat_mul_rows` (bit-identical by S1-4), then stacked across the B streams of a step so the 8.4 MB head is read once per step, not once per frame per stream — **LANDED 2026-09-21** (`ebd91de`), causally screened on the box in F29: arm B (this change plus the dead-row narrowing, rel-pos table OFF) holds C=40 where arm A breaks, emission lag p95 616 → 229 ms. The two effects cannot be separated further by this experiment and the entry says so
- [x] S10-3 **DONE 2026-09-24, qualified as part of the research candidate (see S12-12).** **Level 1 (`MYNAH_ASR_STREAM_PAR=1`: attention+K/V and conv-mid loops over streams on the pool, bit-exact) is a SMALL WIN on Axion, C=112 x2: lag p95 -55 %, fin p95 -52 %, cores 21.6 -> 24.8, audio/wall +1.4 %, throughput per core -12 %; shift fails the lag and finalization gates at C=112 in both reps, level 1 passes them in 1 of 2 (3 of 4 toward a safe-concurrency step). Nothing at C=80. **Level 2** (+ SiLU per stream, residual+LN fused per row) over level 1, ABBA x2: lag p95 **-24 %** more, fin p95 -26 %; cumulative against shift **-66 % / -65 %**, and it passes every serving gate at C=112 in both reps where shift fails in 5 of 5 — the registered safe-concurrency step, paid for with throughput per core 3.80 -> 3.22 and 4x the pool dispatches. Knee probe C=128: shift COLLAPSES (backlog 5.9 s, lag p95 2.8 s) while level 2 degrades gracefully with **+7.6 % audio/wall, +16 % utterances, +8 % throughput per core**: the knee moves from C=96-104 to C=112-128. Level 3 (front end + decode over streams) is neutral at C=112 but **MATERIAL at C=128 on top of level 4: lag p95 -23 %, fin p95 -28 %**; level 4 (activation quantisation in the producing regions, 0 new dispatches) -19 % lag p95 at C=128, just under the bar. Stack 2+3+4 at C=128: lag p95 2781 (off) -> 261 ms, audio/wall 77.5 -> 86.2; finalization (single-stream, 515 ms against 500) is the gate left. Promotion to default is a product decision, not taken** → [`.work/plateau-campaign-2026-09.md`](.work/plateau-campaign-2026-09.md). Was: The elementwise stages the scheduler thread runs alone while the pool idles: `parallel_for` over streams for attention, conv, SiLU, subsampling; activation quantisation by row (the 2^21 threshold is never met); SiLU fused into the qgemm epilogue
- [x] S10-4 **REJECTED 2026-09-24**: spin 50 -> 500 us at C=112 took worker spin hits 48 -> 95 % and burned 5.5 more cores for audio/wall -0.1 % and a lag change inside the shift arm's own run-to-run range; park/wake is not what limits the plateau, and the spin arm filling the missing cores shows they are pool threads waiting for a serial caller → [`.work/plateau-campaign-2026-09.md`](.work/plateau-campaign-2026-09.md). Was: Pool barriers per step counted and the park/spin meter read at C=32, then `MYNAH_ASR_POOL_SPIN_US` swept in SOAK (the sibling: neutral in wave, +11 % p95 in soak); one persistent region per step with spin barriers is the structural answer if the count is the cost
- [~] S10-5 (measured by S13-1d: the `slide` layout, SMALL WIN, not promoted) K/V cache written in place (`[left+Qmax, d]`, compaction every left/Q steps) instead of a 600 KB copy plus a 540 KB shift per layer per stream
- [ ] S10-6 Subsampling stacked across B and its 17.8 MB f32 linear quantised; `xn` quantised once for q/k/v; depthwise weights transposed at load
- [ ] S10-7 VERIFY on the box that `expf` is vectorised in the Linux binary (`objdump` for `_ZGVnN4v_expf`); if scalar, a branch-free sigmoid with the golden gate
- [ ] S10-8 Layer norm and mel front-end out of double: f32 two-pass norm, twiddle table and real FFT, each behind its oracle gate
- [ ] S10-9 The delta framing allocates ~30 times per delta on the scheduler thread and no gate covers it: an arena for cJSON or hand-formatted frames, and a server-side allocation gate beside the two library ones
- [x] Q-1 One CER and one percentile: `test_langs.py` imports `streaming_metrics` (NFKC and full Unicode punctuation move there), `rest_load.py` uses the same `pct`; `load_transcripts` keyed by `lang/file` (today 11 languages collide on `fleurs_1521.wav` and score against the wrong reference) — **LANDED 2026-09-21** (`a822d31`): the normaliser is NFKC + Unicode punctuation, `test_langs` and `rest_load` import it, and the fractional-rank bug it exposed (p99.9 of 1000 samples WAS the maximum) is fixed and pinned
- [~] Q-2 **Observational half DONE 2026-09-21** (`tools/eval/rnnt_earliness.py`, 21 utterances, N-sensitivity reported): correct early evidence exists in 19–62 % of utterances depending on the stability rule, always quantised to one chunk, but the wrong-runner-up fraction RISES with stricter stability (19→25→33 %). Still owed before R-5: first-correct-lexical-token latency, partial revisions, false non-blanks in leading silence. **Blocks R-5** (R-3F): the baseline must also detect a premature or wrong FIRST token, partial instability, and hallucination in leading silence — today it covers final CER only. Committed CER baselines per model, quant and lookahead under `configs/quality/`, and a regression gate with a margin in `test_samples.py` and `stream_load.py --transcripts`; `perf_profile.py validate` refuses `qualified` without one
- [ ] Q-3 Streaming-vs-offline CER delta per lookahead over `samples/`, and CER on REST (`rest_load --reference/--transcripts`), so Parakeet and Nemotron are scored by one report on one corpus
- [ ] Q-4 Dead and duplicate tooling: `tests/test_kquant.sh` (it execs a `test_gguf` binary that has no source and no make rule, so it has only ever exited 77), the tracked `tests/test_ingot_parity` binary, the 94.6 s clip in the `bench-stream-wave` default, five WebSocket client copies, two box orchestrators outside `make`, `perf_profile.py validate` outside `make check`
- [x] T-1 Reporting-only additions to `streaming_metrics.py` from data the JSON already holds: p99/p99.9, audio-seconds per wall-second, end-of-utterance latency, per-stream fairness and max inter-delta gap, WER, bootstrap CI; `getrusage` + `/proc/self/status` in `[DUMP]`; a cross-run comparator that refuses unequal manifests — **LANDED 2026-09-21**: `tools/bench/compare_runs.py`, p99/p99.9 withheld below the n that can name a rank under the maximum, fairness as worst-over-median, longest inter-delta gap, audio per wall second, end-of-utterance latency, WER
- [x] T-2 Tier 0 (~90 s, one command per model): dispatch map, self-tests, one warm server, 4 WS streams with CLI reference and `samples/` CER, 4 REST, dump, zero survivors. Tier 1 (~8 min): knee by bisection on a warm server with validity-length rungs, cold quality pass, REST rungs for the offline model. Tier 2 (~30 min): two identical 600 s soaks at the profile's C with the stress bank and `--transcripts`, `perf_profile.py promote` — **LANDED 2026-09-21**: `tools/bench/tier.sh` + `make tier0/tier1/tier2`, with `tools/bench/knee.py` (bisection on one warm server), `tools/eval/cer_offline.py` (baseline + margin) and `compare_runs.py`; tier 2 refuses a dirty tree and refuses to run without a reference manifest
- [ ] T-3 `make fetch-stress-bank` run once and every soak pointed at it (S4-7): until then no soak scores a transcript and no operating point can be promoted

### R — First-partial responsiveness: the other SLO → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)

Opened 2026-09-21 from F29: the steady state moved by nearly a factor of two
while the wait before the first word did not move at all. The goal is not to
lower 2.6 s, it is to account for all of it. No optimisation before the C=1
waterfall reconciles.

- [x] R-1 Prove from code what every timestamp on the path means and in which clock domain, `stream_open` through `client receives first partial`; name `TTFP-open`, `TTFP-audio` and `TTFP-speech` separately and instrument speech onset rather than substituting stream open for it — **DONE 2026-09-21**: today's `ttfp_ms` is relative to the completion of the first audio WRITE, so it is neither open- nor speech-relative; the harness and the server were on two different clocks on macOS (`mach_absolute_time` vs `CLOCK_MONOTONIC`, 4.63 days apart, a no-op on Linux) and the harness now reads `CLOCK_MONOTONIC`; the server publishes only three instants between audio-in and text-out, so `publication_delay` cannot be computed today; `s->t_first_delta` is written and read by nothing; the batch window, the scheduler tick and Nagle are all cleared as suspects; the first chunk needs 256 ms of audio, not 320
- [x] R-2 One stream, C=1, a millisecond waterfall that reconciles with the measured first-partial latency; report `audio_seconds_consumed_at_first_nonblank` and `wall_time_of_first_nonblank` explicitly. A concurrency effect must not be allowed to hide a fixed pipeline floor — **DONE 2026-09-21** (`MYNAH_ASR_TRACE_TTFP`, `tools/bench/ttfp_waterfall.py`, eight clips, onsets 0.15–3.45 s): **branch B**. `publication_delay` is 0.12–0.23 ms, so the serving path owns none of the wait; the RNNT consumes **0.50–0.97 s of speech** and 2–13 blank steps before its first non-blank, and in 7 of 8 `TTFP-open` is that audio plus one step wall. The pipeline idles 270–291 ms between 30–46 ms steps against a 320 ms period: waiting for audio, not starved. A fresh server's FIRST step costs 1236 ms and blocks two chunks behind it
- [x] R-3 Classify every component of that waterfall as MODEL ARCHITECTURE / DECODING POLICY / OUR IMPLEMENTATION / REAL-TIME INPUT / COLD-START / BENCHMARK ARTIFACT / UNKNOWN. No optimisation proposal is admissible without this classification — **DONE 2026-09-21** (`MYNAH_ASR_TRACE_RNNT`): there is no decoding policy to blame — the greedy is a plain argmax with no threshold, temperature, suppression or commit rule, and the one gate we owned (`total > chars_emitted`, which drops a language-tag-only step) fired 0 times in 3 clips. The first frames are STRONGLY blank (margin ~+100 in silence, +4..+9 after onset) and only the crossing is near-boundary (−0.45..−2.61). Preset `[56,3]` puts 240 ms of right context inside each decision, so the token cannot exist earlier and is then published in 0.2 ms. The wait tracks SPEECH (+0.46/+0.56/+0.79 s) and not silence (0.18→3.45 s, a 19× range). Cold start is UNKNOWN and not reproducible (43.8/49.4/1010.6 ms over three fresh processes)
- [x] R-4 `publication_delay = client_first_partial − model_first_hypothesis`, instrumented as a first-class metric: it is the discriminant that says whether the serving path or the model's own emission policy owns the seconds — **DONE 2026-09-21** → F30: **0.08–0.10 ms** on the Axion, 0.12–0.23 ms on the dev host, at every concurrency from 1 to the knee. The serving output path owns a tenth of a millisecond of the first word and is not a candidate for optimisation
- [~] R-5 **Family B screened and STOPPED at phase 1, no VM started** (2026-09-21): `[56,0]` is the only preset below 3; proved from code that it takes the cadence period 320→80 ms and the first chunk 256→16 ms. It buys a median 100 ms of speech before the first non-blank and costs mean CER 0.0405→0.0525 (8 clips worse, 4 better), so quality disqualifies it before the capacity tax is counted. The hybrid fast-start→normal-cadence idea is recorded as an UNPROVEN hypothesis: a lookahead change closes and reopens the stream today, and a mid-stream switch has no gate. Original scope, family B only (context/cadence). Q-2 ruled out family A: a stably WRONG runner-up is exactly as early (median headroom 300 ms, p95 700 ms) and exactly as stable as a correct one, the margin trajectories are indistinguishable, and one stable runner-up in four is wrong. Smallest reversible A/B on the dominant term only, `before → mechanism → after`, same audio and build but the switch, transcript gate green; never trade recognition quality, transcript semantics, established streams or steady-state capacity for it
- [~] R-6 Load dependence: the DIAGNOSTIC half is done → F30 (TTFP-open p95 1535→1719 ms from C=1 to the knee while emission lag p95 goes 17→268 ms; speech needed before the first non-blank is 0.716 s at C=8/32/65 alike). What remains is the synchronised-opening A/B, which is the only thing that would upgrade F29's TTFP reading from inference to fact. Original scope: C1 → C8 → C32 → near-knee, then synchronised opening on its own, reporting TTFP-speech p50/p95/p99, TTFP-open p50/p95, model first-hypothesis latency, publication delay, emission lag, backlog, loss, CER and audio/wall together, so responsiveness cannot be bought with capacity

- [ ] R-7 **OPEN / UNPROVEN.** State audit DONE 2026-09-22 (source only): `es->right` is read in exactly two places in `src/encoder.c` and never reaches the step, so it is a chunk length and not a mask; K/V, conv, subsampling caches and the rel-pos table carry no memory of it, and a stream opened at `[56,3]` can run its opening chunks at `right = 0` without crossing any buffer bound, which INVERTS the dangerous direction recorded the night before. What the model does with a chunk grid that changes mid-utterance is still unproven and is the whole question; the A/B is not started (no converted pack on this host). Mid-stream lookahead transition: pay the fast cadence only until the first useful partial, then return to `[56,3]`. Today a lookahead change closes and reopens the stream and loses the K/V and conv caches (`sched_ensure_stream`); dimensionally a downward switch would fit, but frames already cached were computed under a different attention window and nothing gates that. **The correctness gate is written before any code**: a stream whose `right` changes mid-utterance without reopening must produce a defensible transcript against both fixed-preset references, the caches must be shown to carry the same meaning across the switch, the scratch and rel-pos table must be proved large enough for both regimes from `mynah_asr_enc_stream_init`, and the dangerous direction (q=1 → q=4) must be covered. If the transcript does not hold at library level, R-7 closes there and no serving change is designed → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)

- [ ] S11-1 **Cold start and readiness — audit done, nothing implemented.** F30 attributed the Linux cold start (3260-3270 ms, 6555-6624 major faults, 6 of 6 fresh processes; warm 36 ms, 0-2). The source audit says exactly two predictable costs are still charged to the first request: the mmapped weight pages (`MAP_PRIVATE`, no `MAP_POPULATE`) and the thread pool (`pool_init` under `pthread_once`, on the first `parallel_for`). Everything else is already paid at load — the rel-pos table deliberately so. The lifecycle hole is that there is NO readiness endpoint: `/v1/health` never says whether this worker has done its first forward, so the first stream pays the 3.26 s. Readiness first (correctness, testable), the warm-up only after a Linux measurement of wall AND major faults against dropped caches → [`.work/cold-start-and-readiness.md`](.work/cold-start-and-readiness.md)

- [x] R-9 **DONE 2026-09-22.** The trace now records, per decision: absolute frame, audio consumed, `|enc|`, `|joint|`, predictor SOS/MOVED, post-intervention flag, and blank / word-mark / best-LEXICAL / best-non-blank each with id, logit and 1-based rank. `▁` resolved via `mynah_asr_tok_find` from the pack's own `tokens.json`, never hard-coded. It immediately answered two things: the old `best_nonblank` WAS the word mark on most pre-first-token frames, and the extreme logits exist identically in f32 (-969 vs int8 -946 on the same frames), so they are not a quantisation artefact. Original ask: `MYNAH_ASR_TRACE_RNNT` prints `blank`, `best_nonblank` and the margin, and `best_excluding` returns the best NON-BLANK — which may itself be the bare SentencePiece word mark. Extend it to carry, per frame, P(blank), P(`▁`) and P(best LEXICAL non-blank) with ids AND ranks, so the sequence `blank >> ▁ > lexical` -> `blank ≈ ▁ >> lexical` -> transition can be seen if it is there and ruled out if it is not. `▁`'s id comes from the pack's `tokens.json`, the map `tools/eval/rnnt_earliness.py` already loads. Prerequisite for R-8 → [`.work/rnnt-first-emission.md`](.work/rnnt-first-emission.md) Also add the blank/best-nonblank LOGIT SCALE to what is recorded: the pre-first-token regime (|blank| ~ 900, margin ~ 97) is numerically odd and it is not established whether it is a real property of the distribution at SOS or an int8 artefact of the head early in the stream.
- [x] R-8 **DONE 2026-09-22 — the hypothesis is REJECTED for this checkpoint.** Holding the encoder regime fixed (`|enc|>10`), moving the predictor does NOT remove the extreme blank regime (median 953 -> 1295, still 100 % above 100): it is an ENCODER phenomenon on early silent frames with a cold cache, made visible by the suppressive SOS `g` (`|g|` 15.46, falling to 2.28 after one more `pred_step`). Injecting blank DELAYS the first natural token (median +4 decisions, CER 0.0286 -> 0.0662, transcript changed in 21/21) and delays it after the first token too (+2 at frame+4, +4 at frame+8), which is predictor-history corruption, not unlocking. Token identity orders the arms — blank +4, best-lexical +1, best-non-blank 0, bare `▁` **-2** — and `▁` is the only arm that never delays (0/21 later, CER 0.0214), but its first published word is right in **10/21 against the baseline's 16/21**, and at a DEPLOYABLE fixed point it gives CER 0.0575, double the baseline. **R-8 rejects an initial predictor-state emission lock. It does NOT establish that the remaining first-word latency is irreducible acoustic-evidence latency** — it examined only the last arrow of `encoder context policy -> cache fill -> lookahead/chunking/subsampling -> encoder representation -> RNNT decision`. The causal search moves UPSTREAM, and the finding that the phenomenon lives in the encoder on the first frames with a filling cache makes that more interesting, not less. Original ask: Diagnostic only, default OFF, development host, no Axion. A second report on the same model family turned **178 of 179 empty outputs into non-empty** by forcing one token into the decoder — an intervention, not a correlation. Our regime is NOT theirs (we have zero empty transcripts; our symptom is 0.50–0.97 s and 2–13 blank steps before the first non-blank), so the question here is the weaker one: is part of that 0.7 s a STATE barrier rather than missing acoustic evidence? The hook already exists — `pred_step()` is the only mutator of predictor state and emission is a separate statement, so "perturb without publishing" is `pred_step` minus the `tokens[n_out++]` line (src/decoder.c:326). Arms A baseline / B best non-blank / C reference-compatible (oracle) / D wrong lexical / E bare `▁` / F unrelated control / **G `dec->blank` itself — the tightest control, since blank normally never advances the state: G vs F decides whether it is the token at all or just the `pred_step`**. Second pre-registered axis: inject at k=1,2,4 decisions before the baseline crossing — a barrier shows dose-response, evidence does not. Point pinned to an absolute encoder frame from the baseline trace, so it is identical across arms by construction. Primary outcome is a COUNT (blank decisions to the next natural non-blank), not a wall time. Run on clips that already work, not only slow ones. Forced token never published; CER scored on visible output only. **Subsequent decoding and final quality are recorded separately and never inferred from each other** — a state unlock that produces fluent wrong text is a finding, not a win. If nothing moves, R-3's attribution stands strengthened → [`.work/rnnt-first-emission.md`](.work/rnnt-first-emission.md) **2026-09-22, saved traces, no model needed:** `tools/eval/rnnt_silence.py` classified 3579 decisions by audio x predictor x encoder cache. **Every cell where the predictor has MOVED is 0.0-0.1 % extreme-blank, whatever the audio and whatever the cache** (638 frames of silence with a moved predictor: zero; 720 frames of a filling cache with a moved predictor: zero). **Every cell at SOS is 18-63 %**, median |blank| 914 against 27. Reproduced cell for cell on the independent `[56,0]` arm. `pred_step()` runs only on an emitted token, so `s->g` is FROZEN for the whole window. Observational, not causal — the cells that decouple SOS from position hold 16 frames across two clips — which is precisely what the injection would fix. Arm G is now the sharpest test.
- [ ] Q-5 **Metrics that would let us judge a fine-tuned pack on the mechanism, not on CER.** Add to the quality gate: empty-transcript rate (today 0/21, and that zero is worth recording), audio and speech to first non-blank, blank/non-blank margin at onset. First lexical token correctness is already in `tools/eval/partial_quality.py`. Without these, a FastEmit-trained checkpoint — FastEmit exists precisely to push a transducer toward earlier emission — would be compared on a WER that hides whether it changed this dynamic at all → [`.work/rnnt-first-emission.md`](.work/rnnt-first-emission.md)

- [x] R-10 **Family B premise check, DONE 2026-09-22 (code + pack, no benchmark).** `tools/eval/preset_derive.py`. The pack declares `[56,3]`, `[56,0]`, `[56,6]`, `[56,13]` — **`[56,1]` and `[56,2]` do not exist here**, and `[56,2]`'s `q=3` does not even divide `left_ctx=56`, breaking the invariant `src/encoder.h` rests its UNMASKED attention on. **"`[56,3]`→`[56,0]` removes 240 ms" is wrong by 2×**: the attention is unmasked over `[cache + chunk]`, so a frame at position `t` of `q` sees the `q-1-t` frames AFTER it — 240/160/80/**0** ms at `[56,3]`, and the LAST frame of every chunk has no lookahead in any preset. The expectation is `(q-1)/2 × 80 ms` = **120 ms**. Measured on 21 saved clips: median −0.100 s, mean −0.129 s. That is why `[56,0]` disappointed. And **cache fill is 4480 ms of audio in EVERY preset** (`ceil(56/q)` steps × `q×80 ms`), so every first token in this corpus is decided with a partial cache and no preset changes it → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)
- [x] R-11 **Within-chunk phase test, DONE 2026-09-22 (saved traces only).** P1 is **untestable**, not falsified: **17 of 21 crossings land at chunk position 0 and none at position 3**, exactly the confound pre-registered in [`.work/chunk-phase-prereg.md`](.work/chunk-phase-prereg.md) — the greedy loop scans a chunk in order and breaks at the first non-blank, so the lowest position wins by construction and the treatment has no variance. New finding instead: **the high-norm encoder regime OCCURS DURING PARTIAL-CACHE OPERATION and is strongly POSITION-DEPENDENT, sparing the last frame of each chunk** (position-dependence is measured; cache-dependence is NOT established — co-occurrence, not cause) (|enc|>10 on 60/52/66 % of pre-crossing decisions at t=0/1/2 against **22 %** at t=3). With |enc| controlled the pre-crossing margins are flat (11.3/11.5/10.0/13.3), so "each frame dominated by its own past" is unsupported. The lookahead IS visible post-crossing — margin 2.26/4.58/4.88/**6.11** over 3101 balanced decisions, monotone in the predicted direction — i.e. in the regime that does NOT set first-word latency. **No number is extrapolated**: the 120 ms cap stands from R-10's derivation, R-11 cannot test it at the crossing, and lookahead is not the explanation for the residual → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)
- [ ] R-12 **Cache priming — OPEN, NOT RUN, specification only.** Inspected far enough to say what it would mean. A prefill is not "filling an empty buffer": the caches are already `calloc`'d and the attention reads only `[0, cache_valid)`, so a prefill is **raising `cache_valid`** — which sits inside the positional arithmetic (`K = valid + Q`, `rel_shift` base `= K-1-valid-t`, rel-pos table indexed by `Kmax-K`), so it changes which relative-position rows every query reads. Zero-fill is out-of-distribution on three counts and is rejected; priming with the clip's own silence changes the audio too and is only an upper bound; duplicating the clip's frames leaks future speech and is excluded. Only priming with unrelated room tone keeps the target audio byte-identical while giving an honest warm cache — and it is still an intervention. As a production optimisation the arithmetic is unforgiving: the cache needs 4480 ms of audio, self-defeating in real time, and faster-than-real-time it costs ~14 steps ≈ 180 ms of CPU per stream open, which lands on the cadence budget. Six conditions must be proven before it runs → [`.work/cache-priming-spec.md`](.work/cache-priming-spec.md)
- [x] R-13 **LOCALISED 2026-09-22 (Mac, EN/FR/IT/DE).** The positions are **identical through the entire conformer stack** — mel 1.00, subsampling 0.98, layers 0–11 1.00 — and diverge for the first time at the **ReLU in the first layer of the prompt projector** (`mid = ReLU([x, one-hot(prompt)] @ prompt_l1_w^T + b)`): input ratio t0/t3 **1.13×**, output **7.21×**, reaching 6.13× at what the joint reads. Post-crossing every boundary is 1.00–1.11×. So the high-norm regime is produced AFTER the last layer norm, not by chunk assembly, subsampling, attention or convolution. HYPOTHESIS from the code, not measured: `x` reaches the concat with RMS ~0.045 while the prompt one-hot is 1.0, so the ReLU pre-activation is dominated by the prompt column and `x` only decides which units survive the gate. **NOT established that high norm is bad** — no normalisation, clamp or decoder compensation until it is shown to predict a delayed/wrong first correct lexical token or CER/WER, EN and FR independently → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)
- [x] R-14 **ASAP/MAX PRIORITY — RESULT 2026-09-23, Axion, from the FROZEN C=80 artefacts, nothing re-run.** The qualification already carried the four fields R-1 specified, so the first cut cost no compute. Paired per clip over the 498-clip unloaded pass, `ttfp_from_speech - speech_consumed` lies in **[-78, +7] ms** — the wait before the first word IS the speech the model consumes, measured on the two sides of one socket. **Load does not move it**: speech consumed p95 is 1996 ms unloaded and **1986 ms in both C=80 soaks** (21719 utterances), a 0.5% difference over a 1->80 range, while the only term load moves is the server's lateness, 20 -> 105 ms p95, the same +86 ms the paired gate measured independently. Publication is **exactly quantised to the chunk grid**: `first_delta_audio_s` takes 12 distinct values over 498 clips, every one on `0.256 + 0.32k`, and **k=0 and k=1 never occur** — nothing publishes before 0.896 s of audio; 93% publish by k=7. Median speech consumed is flat at ~500-960 ms across 96.8% of clips, so the 1986 ms p95 is a 1.6% tail (k>=10, 3.2-4.2 s), not the body. **Component D is closed at 20 ms idle; A+B own the rest; C is not yet measured** -> [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)
- [x] R-15 **DONE 2026-09-23 — the publication gate holds NOTHING; component C is exactly zero.** 120 clips, Axion, `MYNAH_ASR_TRACE_RNNT`, audio-time not wall-time, two independent sources cross-checked (the per-decision trace on stderr and the `--deltas` a client would see on stdout). `speech -> first non-blank decision` and `speech -> first published delta` are the SAME distribution to every digit (p50 **860 ms**, p95 **1990 ms**, min 380, max 4250), so `publication_gate_ms` is **0.0 at every percentile on 120 of 120 clips**: the append-only contract at `src/mynah_asr.c:1004` does not delay the first word by one chunk. **DECISION: the two-tier provisional/stable API branch is killed** — there is no early hypothesis for a provisional channel to carry. Cross-validates F32 across two harnesses, two audio grids and two samples (server read p50 816-876 / p95 1986-1996). The decoder decides once per 80 ms encoder frame and needs ~17 of them (min 8, max 56). One column was **withdrawn before reporting** as a parser artefact, not explained away -> [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)
- [x] R-16 **FIXED 2026-09-23.** Both consumers now read the current line through one `parse_frame()` per file, with the old->new field mapping written down (old `margin` is `margin_nb`; old `chose=TOKEN` is `chose=<id>` differing from the blank id on the same line; old `blank` is the SCORE inside `blank=<id>:<score>:r<rank>`, not the id). `tests/test_rnnt_trace_parsers.sh` pins it with **real captured output of the current emitter**, not lines written by hand against the printf, and asserts a blank decision and a token decision are told apart, that the emit line and a non-trace line are REJECTED rather than half-parsed, and it is wired into `make check`. Was: `tools/eval/rnnt_earliness.py:36-38` and `tools/eval/rnnt_silence.py:59-62` still parse the pre-R-9 `blank=... best_nonblank=...:... margin=...` form, which `dec_trace_line` (`src/decoder.c:310-343`) stopped emitting when R-9 extended it; only `tools/eval/norm_predicts.py` matches the current line. A tool that silently parses nothing is worse than a missing tool, because it reports an empty result as a finding. Fix the parsers against the emitter and add the assertion that a run over a known trace yields a non-empty set
- [ ] C-1 **Campaign ledger** — FACT / HYPOTHESIS / EXPERIMENT / RESULT / DECISION / NEXT, one row per decision, so a new session inherits verdicts instead of impressions. Rejected hypotheses stay in it: re-proposing one costs more than a row → [`.work/campaign-ledger.md`](.work/campaign-ledger.md)
### Later, not now
- [ ] L-2 Unstable-tail partials for Nemotron (a decoder change; protocol already has `final`)
- [ ] L-3 CUDA v2b resident encoder (from the v1 TODO; GPU serving is out of v2 scope)

### S13 — NVIDIA ASR architecture / streaming laboratory **[ASAP]** → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)

Opened 2026-09-23 on `research/asr-streaming-lab`, after the C=80 qualification
was frozen on `main`. **This phase begins with analysis, not implementation**:
no new backend is written until the audit says what needs to exist. The frozen
C=80 result and the production profiles are the immutable reference point and
are not recomputed here.

- [ ] S13-1 September-2026 NVIDIA ASR ecosystem audit from PRIMARY sources (model cards, configs, NeMo code, NIM docs, papers), with a factual matrix and UNKNOWN wherever the source is silent. Community material is secondary and labelled. Independently verify the three claims on the table: EOU 120M is cache-aware FastConformer+RNNT 17L `[70,1]` ~120M EN-only with a native `<EOU>` and 80-160 ms operation; TDT v3 covers 25 European languages including EN and FR; Nemotron 3.5 is multilingual cache-aware streaming
- [x] S13-2 **Checkpoint architecture forensics, DONE 2026-09-23 for all five models, for 6 MB of transfer.** A `.nemo` is a tar whose FIRST member is `model_config.yaml`, so an HTTP range request over the first 3 MB reads a whole architecture without fetching the checkpoint — the 460 MB EOU 120M and the 2.49 GB multitalker were never downloaded and no Axion disk was touched. **All five instantiate the same `ConformerEncoder` + `RNNTDecoder` + `RNNTJoint`: there is no separate streaming encoder class, streaming is switched on by configuration.** Offline-versus-streaming reduces to `att_context_size` (`[-1,-1]` against a finite pair), `att_context_style`, `causal_downsampling` and `conv_norm_type`, plus the mel `normalize` mode. Nemotron 3.5 is trained MULTI-context (`[[56,3],[56,0],[56,6],[56,13]]`); the EOU 120M declares one, `[70,1]` — half the cadence at 160 ms, left context 70, and `q=2` divides 70 so R-10's invariant holds → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [x] S13-3 **DONE 2026-09-23 — it converts, loads, streams and agrees with the oracle, with ZERO changes to `src/`.** The config analysis predicted it before the code was written: the pack passes all six gates of `mynah_asr_stream_unsupported()`, has no prompt projector (and mynah already carries the no-prompt branch at `src/encoder.c:594`), and its 17 x 512 encoder with a 1-layer predictor is a shape already run. The gap was ONE cell of the converter's matrix — cache-aware checkpoints arrive by the HF-native route, and this one ships a `.nemo` and nothing else — so `build_streaming_rnnt_from_yaml` is about eighty lines in an offline tool. Result: **465 tensors, 1027 pieces**, `_NEMO_RENAMES` covered every key, offline RTF 0.139 f32, **18 incremental deltas cache-aware**, streaming text identical to offline, int8 identical to f32, and **`oracle == offline == streaming` byte-identical on 4 of 4 committed English clips**. Output is lowercase and unpunctuated, exactly as the card says — a cheap identity check. ENGLISH ONLY; **not** a Nemotron replacement (documented WER 9.30 at 160 ms against Nemotron English 7.67, and no punctuation or capitalisation). Was: It passes **all six** of `mynah_asr_stream_unsupported()`'s gates (`use_bias: false`, `causal_downsampling: true`, `conv_norm_type: layer_norm`, `xscaling: false`, mel `normalize: NA`, subsampling 8), it has **no prompt projector** and mynah already carries the no-prompt branch (`src/encoder.c:594`) because offline Parakeet needed it, and its 17 x 512 encoder with a 1-layer predictor is the shape mynah already runs. **That makes "importer and config work only" a HYPOTHESIS, not a result**: tensor names and layout inside the `.nemo`, what `tools/convert_nemo.py` expects, and numeric parity against the oracle are all unproven and no transcript has been produced. ENGLISH ONLY — it does not replace multilingual Nemotron
- [ ] S13-4 Parakeet TDT v3 multilingual: (a) serving-level buffered/prefix pseudo-streaming measured honestly as such and never called cache-aware — prefix ladder 250..3000 ms on FLEURS EN **and FR**, reporting first correct word, stable-prefix length, revision rate, agreement with the full-utterance transcript, WER/CER and RTF per prefix; (b) READ-ONLY feasibility of a streaming fine-tune, given S13-2's finding that the encoder difference is configuration. No training is started
- [x] S13-5 **THE CONTROL ANSWERED 2026-09-23, and the fork is taken on the EARLIER branch.** Both models, the same 120 clips, same onsets, same committed tool, same box and session, both int8, measured in AUDIO time. **The 120M is earlier on 112 of 120 clips, later on 1, tied on 7, sign test z = +10.44**, median **-300 ms** and essentially never materially later; speech to first non-blank goes 860 -> **570 ms** p50 and 1990 -> **1590 ms** p95. **The gain decomposes into two measured halves**: the model commits after **2.2 fewer decoder decisions** (paired), which at 80 ms per encoder frame is about -176 ms, and it publishes on a grid twice as fine (9 distinct audio values against 17, minima 0.900 s against **0.600 s**), worth about -150 ms. So a shared frontend or runtime assumption is RULED OUT as the dominant cause — the same frontend and runtime produced both numbers. Candidates are now specific and two are documented: NeMo ships FastEmit lambda **3e-2 for the EOU model against 5e-3 for cache-aware streaming**, and FastEmit is training-time only; plus the cadence. Which one produces the decision-count half is NOT established. **R-15 also reproduces on this second checkpoint**: publication gate 0.0 at every percentile. Continued from R-14/R-15. Components C (publication, 0 ms) and D (serving, 20 ms idle / ~105 ms at C=80) are CLOSED. What remains is A versus B — of the ~17 blank decisions before the first word, how many are absent evidence and how many are evidence not committed — plus the tail: 96.8% of clips sit at 500-960 ms of speech consumed and the p95 is produced by the 1.6% at `k >= 10`, which are not characterised. **The EOU 120M is the control that removes the prompt projector R-13 implicated**
- [x] S13-1c **NeMo-Speech.cpp audited 2026-09-23.** It is real, first-party, Apache-2.0, pushed today, and built on ggml + llama.cpp submodules — with the same four q8_0 checkpoints plus diarization, NMT, TTS and full-duplex S2S; its own docs say NIM is the supported production path and this server is for local use. **It independently made our central call**: its own `fastconformer.h` (lines 114-121, in THEIR tree) says it caches projected K/V *"rather than the pre-projection layer input"*, departing from NeMo Python exactly where we did. It is AHEAD on batching breadth (frontend, encoder, **predictor**, **joint**, TDT, VAD, PnC, plus an ingress cohort coordinator that releases on wave arrival rather than a timer). It is BEHIND on everything that makes a CPU box serve 80 streams: no thread pool of its own, **one mutex serializing every ggml graph** (their own doc), HTTP pool default 4, no admission control beyond a thrown exception, **zero hand-written CPU SIMD** with an open **SIGILL** on AVX2 hosts (#23) — the failure our `src/dispatch.h` guard turns into exit 78 — batching parity as a **5% edit-distance tolerance** rather than byte identity, a Python-oracle parity suite for the diarizer only, and **no published performance number of any kind** → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [x] S13-1d **CACHE-RING-1, DONE 2026-09-24: the memmove is REJECTED as a cause of the core plateau.** Three bit-exact layouts behind `MYNAH_ASR_KV_LAYOUT` (default `shift`). On Axion at C=112, `slide` removes 93 % of cache copying (4.5 -> 0.3 GB/s) and buys **+1.7 % audio/wall, +0.65 cores, lag p95 -18.6 %, backlog -20.5 %** over two interleaved reps each: a SMALL WIN on overload latency, fully explained by the direct 2.1-2.4 % of encoder time the profile already showed; `ring` (memmove only) NEUTRAL; nothing at C=80 or C1; parity intact everywhere → [`.work/cache-ring-1.md`](.work/cache-ring-1.md)
- [x] S13-1e **REJECTED 2026-09-24 without code or Axion time: the wait it would remove does not exist in the qualified path.** The frozen fleet runs `--batch-window-ms 0` (`tools/bench/v2_qualify.sh:192`, and the profile records why: 40/80 ms windows widened the ready set without moving the ceiling), and `sched_collect()` returns at once when the window is 0, so a step starts the moment anything is ready. With a window, early release ALREADY exists (`ready >= live`, woken on each arrival, first chunks and finalizations exempt); the one residual difference from NVIDIA's cohort coordinator — targeting streams DUE this period instead of all LIVE streams — only matters in a configuration production does not use. Side observation from the CACHE-RING-1 C=80 health snapshot (one worker): ready_mean 1.52, 64 % of steps at B=1, step wall 20.9 / 36.9 / 52.6 / 69.1 ms at B=1..4, i.e. a fixed cost of roughly 5 ms against ~16 ms per row → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [x] S13-5b **TTFP A vs B, 2026-09-24: mostly A.** Raw traces, same 120 clips, both models, no new instrumentation: Nemotron's eventual first token is already the best non-blank 3 and 4 decisions early on 42 / 27 % of clips (EOU 17 / 16 %), a few nats under blank. A blank bias until the first token, replayed exactly on the stored logits, recovers p50 300 ms / mean 213 ms at delta 4 (speech->first 860 -> 680 ms, EOU 580) with 10 / 120 first tokens changed; WER after the first token is UNKNOWN until a live debug-knob run. The "2.2 decisions + grid" split of S13-5 overlaps and must not be added → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [x] S13-5c **Live blank bias REJECTED as a first-word lever, 2026-09-24.** `MYNAH_ASR_BLANK_BIAS` (research, default off) reproduces the S13-5b replay exactly (0 first-frame mismatches at every delta on 349 originals). It moves the first text FRAGMENT forward (p50 840 -> 660 ms, p95 1820 -> 1430 at delta 4) but never the first COMPLETE word (p50 1140 ms at every delta), and WER rises monotonically (0.1040 -> 0.1085). The p95 tail is bound by completing the word, not by the first commitment → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [ ] S13-6 Design study only: does the runtime hard-code "Nemotron means streaming, Parakeet means offline"? Record the technical debt and evaluate a capability model (offline / buffered_streaming / cache_aware_streaming / rnnt / tdt / ctc / multilingual / language_conditioned / punctuation / timestamps / native_eou / speaker_conditioned / multitalker). **No server refactor**; the question is whether it removes future special cases without weakening the optimised paths
- [x] S13-3c **Quality on the 498-clip bank, DONE 2026-09-23 — and the corpus-wide number was an artefact of the CORPUS.** Validity first: the Nemotron arm reproduces the FROZEN C=80 reference scores to four decimals (0.10634/0.14618 against 0.1063/0.1462), so the harness is sound before anything is read out of it. 53 transcripts came back under half the reference length and **all 53 are synthetic `cat2` concatenations, zero are original recordings** (truncation rate 35.6% against **0.0%**); duration is not the cause, since truncated clips run 16.9-21.2 s while intact ones reach 42.4 s. **Measured, not assumed**: on those 53 the 120M scores WER 0.6314 against the full two-utterance reference and **0.1516 mean / 0.0769 p50 against the reference PREFIX of the same length** — it transcribes the first utterance correctly and stops at the seam, which is EOU working. **On the 349 ORIGINAL recordings: Nemotron 0.1040 mean / 0.0667 p50, EOU 120M 0.1164 / 0.0769.** The bank is structurally unfair to an EOU model and `earliness_cost.py` grew a `--drop-composed` flag whose help text says why → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [x] S13-3d **THE HEAD START IS NOT PAID FOR. DONE 2026-09-23, paired on the 349 original clips.** The 120M is earlier on **311 clips, later on 4, tied on 34** (z=+17.30, median **-300 ms**) and costs **+0.0124 WER on the mean with a median delta of exactly zero** — on 183 of 349 clips the two score identically (worse on 104, better on 62, z=-3.26). **Spearman rho(delta first non-blank, delta WER) = +0.1198, z=+2.24: POSITIVE**, i.e. the clips where it saves most time are the clips where its WER delta is lowest. By head-start quartile the mean WER delta is **-0.0001 where it gains 500 ms** and **+0.0283 where it gains only 100 ms** — the relationship runs OPPOSITE to a latency-for-accuracy trade. OBSERVATION and not a mechanism: the reading consistent with this is that a clip hard for a 120M model is hard in both ways at once, and two checkpoints differ in depth, width, data, objective, cadence and vocabulary simultaneously, so it is a property of the pair and licenses an experiment on ONE model, not a causal claim
- [ ] S13-7 EOU / turn-taking primitives. **Already demonstrated by accident on our own corpus**: the 120M ended the utterance at a turn boundary, correctly, 53 times out of 149 opportunities (S13-3c). `<EOU>` and `<EOB>` are ordinary vocabulary entries in the 120M (`num_classes` 1026 against the 110M's 1024), so end-of-utterance comes out of the RNNT joint like any token and costs no new inference machinery — what is new is the CONTRACT of surfacing it as an event distinct from text. Document what the model provides before designing any `ASR_EVENT_*` API
- [ ] S13-8 Multitalker / diarization architecture study, read-only. Its encoder is Nemotron-shaped (24 x 1024) and also passes all six gates; its config declares `freeze_diar: true`, `spk_supervision_strategy: rttm`, `num_speakers: 4`. Establish the diarizer contract, where speaker kernels are injected, per-speaker state, overlap handling, speaker-count scaling and the CPU implications. No implementation
- [ ] S13-9 CPU serving implications and the ranked backlog: QUICK EXPERIMENT / SMALL / MEDIUM / RESEARCH / NOT CURRENTLY JUSTIFIED, each with expected information gain, effort, Axion time, disk, product relevance, **EN+FR relevance** and the unknowns. EN+FR is the product minimum, so an English-only model is never recommended as a Nemotron replacement. Also carries the open cross-model question: CPU occupancy plateaus below the allowed cpus on BOTH models and both serving paths — mechanism UNKNOWN

### S12 — Qualify SERVER V2 as a product → [`.work/server-v2-qualification.md`](.work/server-v2-qualification.md)

The question four days of engine work did not answer: **what concurrency can we
promise continuously, without stalls, dropped established streams, corrupted
output or unbounded latency?** `configs/perf/axion-c4a-highcpu32-nemotron-streaming.json`
has said `status: screened` since 2026-09-20 and says why itself — the C=32
600 s soak passed every serving gate, lost no stream, and did not qualify
because transcripts were never checked. C=16 is the hard product minimum; on
32 vCPU it is expected to qualify with margin, and that is the claim under test.

- [x] S12-1 The candidate frozen and the twelve bounds registered BEFORE the first run: four are the profile's own gates quoted rather than re-derived, eight are new because the previous soak had no equivalent — per-window drift, monotonic trend, server-side stall from SIGUSR1 dumps, a client-observable max emission lag, byte-identical transcript parity under load, RSS growth, worker deaths. TTFP deliberately gets no bound → [`.work/server-v2-qualification.md`](.work/server-v2-qualification.md)
- [x] S12-2 `tools/bench/v2_qualify.sh`: server and load generator pinned to disjoint slices with every worker mask PROVEN inside the server slice, a fresh fleet per rung and per soak, the unloaded C=1 reference captured from the SERVER, SIGUSR1 dumps and RSS/roster samples every 30 s, the corpus identified by hash. It refuses a dirty tree, an UNKNOWN dispatch row, an escaped mask, a reference with holes and a reused reference that does not cover the corpus
- [x] S12-3 `tools/bench/v2_verdict.py` + `tests/test_v2_verdict.sh` (19 assertions): the verdict is a conclusion drawn from artefacts, a bound with no evidence is INCONCLUSIVE and never a pass, the worst WINDOW is checked beside the pooled percentile, and the lag bound is `(lookahead+1)x80` from the run's own manifest rather than the literal 320
- [x] S12-4 `tools/bench/v2_promote.py` + `tests/test_v2_promote.sh` (10 assertions, 8 of them refusals): promotion requires two independent soaks, same concurrency, same bank, DIFFERENT seeds, each over ten minutes, every bound PASS — and the operating point carries the WORSE run in every column
- [x] S12-5 Ladder C=8/16/24/32 on Axion, 90 s per rung, fresh fleet each: emission lag p95 **55/71/73/73 ms** against a 320 ms bound, backlog max 0.184 s against 0.640, **zero** stalls at every threshold over ~14,700 published deltas, and transcript parity PASS at every rung — 27 clips, identical across streams and to the unloaded reference
- [x] S12-6 **DONE 2026-09-22. QUALIFIED.** Two independent 1800 s soaks at C=16, fresh fleet each, different seeds: 7738 utterances, 56488 audio-s, **0** established streams lost, emission lag p95 **65/66 ms** against 320, worst 60 s window 71 ms in both, trend +1.9%/-0.6%, RSS 1.007x/1.009x, no worker replaced, **0 stalls over 125948 published deltas** (worst single delta 233 ms), and **transcript parity PASS** — 27 clips byte-identical across streams and to the unloaded C=1 reference. The two runs agree to 1 ms of p95 → [`.work/server-v2-qualification.md`](.work/server-v2-qualification.md)
- [x] S12-7 **DONE 2026-09-23.** The knee, found on a corrected ladder after `--http-threads` fixed the connection ceiling. Frozen 3x8 on 24 cpus: clean to **C=56**, marginal at 64 (backlog 0.644 against 0.640), clearly bad at 72. Topology compared AT the transition and one rung below: 3x10 buys nothing (45.69 against 45.72 audio/wall at C=56; 53.72 against 53.70 at C=64), 2x15 collapses (lag p95 2007 ms at C=64), 5x6 and **6x5 on 30 cpus** win outright. 6x5 is clean to **C=96** and bad at **104**, reaching 90 audio/wall against 3x8's 56 — the gain is more DOMAINS, not more threads per domain → [`docs/serving-findings.md`](docs/serving-findings.md) F31
- [x] S12-7b **The load generator needed 2 cpus, not 8.** Server frozen at 3x8 in all three arms with the recovered cpus left EMPTY, so nothing could be attributed to two changes at once: 0.09 cores used at 8, 4 and 2 cpus, offered load within 0.5%, and the 8-core arm had the WORST send lateness. Six cpus recovered → [`docs/serving-findings.md`](docs/serving-findings.md) F31
- [x] S12-7c **ANSWERED 2026-09-24 in the research build: serial work on each worker's scheduler thread** (per-stream encoder stages, front end + decode, activation quantisation, and finalization on the single-stream path) while the pool waited. `MYNAH_ASR_STREAM_PAR=4` + `MYNAH_ASR_FIN_STACK=1` (both bit-exact, default OFF) take the fleet to **28.3 of 30 cores** and the knee from C=96-104 to **C=144-160** (~110 audio-s/s); rejected on the way: K/V memmove, pool spin, batch window. Promotion is a product decision with its own qualification → [`.work/plateau-campaign-2026-09.md`](.work/plateau-campaign-2026-09.md). Was: **Why ~7 core-equivalents go unused at saturation.** Measured cpu-seconds put the fleet at 23.1 cores of 30 at C=88 and 23.5 at C=96, and FEWER under overload — 22.9 at 104, 21.5 at 112, 20.8 at 120. Parallel efficiency inside the step, synchronisation, memory bandwidth, GEMM shape and serial sections are all candidates and none is measured. Do not reopen this before the qualification is banked
- [~] S12-9 Promote the profile to `qualified` from the artefacts. **The measurement passed; the archive is what is missing.** The instance became unreachable (no ICMP, port 22 filtered) before `~/asr-evidence/v2/` could be copied, and `v2_promote` derives a promotion from artefacts and refuses without them — hand-writing the block it would have written is what that tool exists to prevent. One `scp` and one command once the box returns; if the instance was deleted, the raw per-utterance records are gone and only the verdict table in the note remains
- [ ] S12-10 Re-test batching now that the ladder is valid AND `/v1/health` is captured per rung (the derived batch column was withdrawn: `steps` counts per slot, so `audio_s/steps` is the chunk period by construction and read 0.98 at every rung across a 2.1x throughput range): 97.5% of steps run at B=1 (`ready_mean` 1.03) because `--batch-window-ms 0` fires on the first ready stream, and the by-B table says 15.46 ms/stream at B=1 against 12.43 at B=4. The profile's record that a 40 ms window did not move the ceiling predates the F15 stall fix and used a different corpus
- [ ] S12-11 Recompute `runnable_idle` (0.223, 99.9% of it in `park`, `ready_sel` p99 50 ms) from the raw dumps before calling any of it recoverable. This profile has already been wrong about this exact figure once, by charging the finalization tail to it, and the correction left 1-3%
- [x] S12-12 **Research candidate QUALIFIED at C=128 and C=144, 2026-09-24** (2 x 30 min fresh-server soaks each, registered bounds, v2_verdict; build `dbae3fe`, `MYNAH_ASR_STREAM_PAR=4 MYNAH_ASR_FIN_STACK=1`). C=128: lag p95 129 ms, fin p95 221 ms, 123.7 audio-s/s on 26.2 cores; C=144: lag p95 243 ms, fin p95 428 ms (thin), 135.4 audio-s/s on ~27 cores; 0 streams lost, transcript parity PASS, transcripts byte-identical to the shipped build on 498/498 (WER unchanged). **qualified_safe_concurrency = 144** (shipped build: 80); screening first bad C=160 → [`.work/plateau-campaign-2026-09.md`](.work/plateau-campaign-2026-09.md)
- [ ] S12-13 **P2 (after the P0 failure work S12-17..20 and P1 S13-10). DECISION PENDING (user): promote the candidate to the production default.** `v2_promote` dry-run accepts both runs; nothing applied. If yes: `v2_promote --apply` on the C=144 (or the wide-margin C=128) run, the profile carries the flags, and the operating point is chosen explicitly (144 with thin finalization margin, or 128 with wide margins)
- [ ] S12-14 After promotion, simplify: make the promoted levels unconditional and delete what did not earn its place — STREAM_PAR level 1 alone, the level ladder itself, `MYNAH_ASR_STREAM_PAR_DECODE`, the `ring` K/V layout (neutral); keep `slide` only if S10-5 is taken up. Every gate stays (`test_stream_batch`, `test_kv_layout`, CLI byte-identity)
- [x] S12-15 **DONE 2026-09-24**: `tools/client_report_axion_asr_20260924.py` (reportlab, same components as the 23 Sep report), 5-page PDF, a 10-clip sample archive chosen by a registered rule, and a full evidence bundle with README/SUMMARY (all delivered outside the repo). Client report and sample archive for 2026-09-24 (qualification at 128/144, the plateau explained, quality by identity, first-text result), same generator family as the 2026-09-23 PDF; evidence bundle `.work/evidence/report-bundle-20260924/` (untracked)
- [ ] S12-17 **P0 TOMORROW (2026-09-25), first item. Harden the failure accounting before any further qualification.** Audit 2026-09-24: the qualification zeros held when recounted from raw records, but the harness has three real gaps that could hide failures: an utterance closed without a `done` frame (or with no events) counts as OK; warm-up errors are dropped before `errors` is counted (bound 1 never sees them); SOAK never asserts every client process lived. Plus the stride artefact (C multiple of 3 locks a stream to one length class). Each fix with a planted-fault case in the `streaming_metrics` self-test → [`.work/failure-accounting-and-cancellation.md`](.work/failure-accounting-and-cancellation.md)
- [ ] S12-18 **P0.** Granular server- and client-side counters (`accepted`, `completed`, `client_disconnect`, `server_disconnect`, `cancelled`, `rejected`, `timeout`, `protocol_error`, `worker_failure`, `internal_error`, `active`) and the conservation invariant `accepted = completed + cancelled + failed + active` checked per window and per run, so a request cannot vanish statistically → [`.work/failure-accounting-and-cancellation.md`](.work/failure-accounting-and-cancellation.md)
- [ ] S12-19 **P0.** Cancellation lifecycle audit in the code, file:line: socket disconnect -> request cancel -> scheduler/slot cancel -> model work stops -> state freed (slot, K/V + conv caches, mel/VAD buffers, output ring, decoder state), for clean close, RST mid-utterance, RST during finalization, timeout, server cancel, worker death. No assumption that closing the WebSocket stops the inference: a zombie step on a CPU server burns compute for a client that is gone (already seen in qwen-tts after a dropped connection) → [`.work/failure-accounting-and-cancellation.md`](.work/failure-accounting-and-cancellation.md)
- [ ] S12-20 **P0.** Fault injection with invariants: client RST mid-utterance and during finalization, idle-open client, malformed WS frames, premature server close, 4xx/5xx/503 and overload rejection, worker death, server SIGTERM/restart, client process death. Per case: slot/request released, no zombie inference, no live state left, the right counter incremented, other streams unperturbed, RSS and active count back to baseline. Then a 10-15 min fault-injection soak at C=64/80 with a deterministic abort percentage at different points: zero orphan work, zero slot leak, no RSS growth, capacity recovered. Then apply the same to qwen-tts (turn its known zombie case into a regression test) → [`.work/failure-accounting-and-cancellation.md`](.work/failure-accounting-and-cancellation.md)
- [ ] S13-10 **P1 tomorrow, after S12-17..20.** First word ~2.3 s p95 (first complete word, 349 originals): serving (+~100-200 ms paired) and the decoder commit policy (S13-5c) are ruled out; investigate the model side — lookahead preset, cadence, checkpoint (EOU 120M is ~300 ms earlier, English only) → [`.work/nvidia-asr-streaming-landscape-20260923.md`](.work/nvidia-asr-streaming-landscape-20260923.md)
- [ ] S12-16 Open items carried from the report: a VALIDATED FRENCH evaluation set (the stress-en bank is English only — EN+FR is the product minimum and FR is unmeasured); the load generator on a second machine; the same campaign on x86; soak the remaining 39 languages of the pack


## Durable contract (changes only when a decision changes)

1. Config-driven: every model number comes from the converted pack's
   `mynah.json`; no model constants in `#define`s.
2. One code path for offline and streaming, and for REST and WebSocket in the
   server: a file is a slot fed faster than real time.
3. Every numeric stage is validated against the Python oracle
   (`tools/oracle/`) before anything is built on it; per-stage tolerances,
   transcripts byte-identical.
4. A stream's transcript never depends on who it was batched with: every
   batched path ships with a bit-exactness gate against the single-item path.
5. CPU is the product. Linux x86-64 and ARM64 are production; macOS numbers are
   development signals. A backend claim names its gates (rule 4 of
   `ENGINEERING.md`).
6. Load never enters the chunk size. When the machine is full, refuse.
7. Python is offline tooling only (`uv`); never required at runtime.
8. Everything in the repo is English. Commit messages: imperative, what and why.
