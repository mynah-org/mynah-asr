# Mynah ASR — plan

`ENGINEERING.md` is normative. This file is the **board**: one line per work
item, linking the `.work/` note that holds the detail. Read the note when you
pick the item up; never write detail here. Legend: `[ ]` open · `[~]` in
progress · `[x]` done · `[-]` dropped. `python3 tools/check_plan.py` must pass.

## Mission

A native C runtime for NeMo speech models (Nemotron streaming, Parakeet,
Canary) that is **usable as one concurrent server on a CPU box**: tens of
real-time streams and offline requests per host, several models in one fleet,
no latency spikes, no stalls, every claim provable from the process itself.
**Production is Linux x86-64 and ARM64 and every efficiency decision is taken
there; macOS is the development machine, nice to have, never the target.**

## Current trusted state (2026-09-18, HEAD after `5f0f802`)

- Runtime 0.9.1: six model families, streaming ≡ offline byte-identical,
  oracle parity per stage, int8/int4 (NEON SDOT, AVX-512 VNNI), Metal/CUDA
  opt-in, VAD, LID, word timestamps, GGUF import. Single-stream RTF 0.02–0.15
  on Apple Silicon and EPYC (`docs/benchmarks.md`).
- The server is a **prototype**: blocking pool, one thread per stream, ceiling
  = `--threads`, no timeouts, no metrics, one model per process. **No number
  exists for N concurrent streams on any host.** Audit and design:
  [`.work/serving-v2-design.md`](.work/serving-v2-design.md).
- Method adopted from qwen-tts / mynah-tts on 2026-09-18:
  [`.work/engineering-method.md`](.work/engineering-method.md).

## Work board

### Reference — read before starting anything
- [x] The v2 serving design, what it borrows, what is ASR-specific, and the
      ideas the siblings already falsified → [`.work/serving-v2-design.md`](.work/serving-v2-design.md)
- [x] The method → [`.work/engineering-method.md`](.work/engineering-method.md)
- [x] What the siblings measured as wins and losses, their flag machinery, kernel state per ISA → [`.work/sibling-wins-and-flags.md`](.work/sibling-wins-and-flags.md)
- [x] v1 history (Italian, closed, local only): the old plan and TODO live in the untracked private archive folder under the work notes

### S0 — Baseline (trimmed 2026-09-18: the v1 server is not worth measuring; S1/S2 start now)
- [ ] S0-6 **The box day**: the ordered runbook for the Linux host — dispatch proof, identity gates, the two questions macOS could not ask, the qualification, multi-model → [`.work/box-day-plan.md`](.work/box-day-plan.md)
- [~] S0-1 Build, gates and single-stream step cost (Q=4, int8, T=1..4, pinned, idle box) on the 32-core ARM host → [`.work/axion-first-run.md`](.work/axion-first-run.md)
- [~] S0-2 Streaming load tool v0: N WebSocket clients paced at 1x, stdlib only → [`.work/bench-harness-streaming.md`](.work/bench-harness-streaming.md)
- [-] S0-3 The v1 server under N streams — dropped: it is broken by construction and the number would not size v2 → [`.work/baseline-streaming-concurrency.md`](.work/baseline-streaming-concurrency.md)
- [ ] S0-4 Allocation count per chunk on Linux, measured not assumed (needs no box: any Linux) → [`.work/baseline-streaming-concurrency.md`](.work/baseline-streaming-concurrency.md)
- [~] S0-5 **Linux functional gates on every server change**: the GitHub CI runs on ubuntu x86, ubuntu ARM and macOS including the prefork phases, ASan+UBSan and clang-tidy; and on 2026-09-19 the whole model-free suite plus the batched identity gates ran on the Axion itself under gcc — a second ISA and a second compiler for the determinism, tiling and pool gates. Still owed: masks read back from the kernel on a pinned prefork run → [`.work/axion-first-run.md`](.work/axion-first-run.md)

### S1 — Library seams the server needs → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [x] S1-1 `mynah_asr_stream_reset` + `need_samples`; slots are pooled, not reopened (gate green 2026-09-18)
- [x] S1-2 Deltas carry a real window: `t0` is the previous delta's `t1`, so the deltas PARTITION the stream and a client places text in time without accumulating anything of its own (it was always 0.0, which made `t1` the only usable field); `is_final`/`is_eou` were already there, and an `eou` is now a point (`t0 == t1`) rather than a window starting at 0. The WebSocket `delta` frame carries `t0`/`t1` beside `audio_s`. The chunk-arrival passthrough is NOT a library API: the server stages the chunk, so it owns the arrival — `emit_ctx.arrival` comes from the slot's own arrival record and the callback runs inside that step (`server/sched.c`). A second copy in the library would be a number the library cannot check → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [x] S1-3 Allocation-free chunk: per-stream scratch, incremental detokenisation — 87 -> 0 allocations per chunk on macOS/Accelerate, gated by `make test-stream-allocs`; the Linux count stays S0-4
- [x] S1-4 Batched stream step for B slots, byte-identical to B single steps: `mynah_asr_stream_step_batch` stacks the ready chunks as one `[Σq, d]` pass; int8 exact by construction (native per-row dot extended to any T, the dequant fallback never reached), f32 measured row-stable on Accelerate and OFF on OpenBLAS until its own gate runs; `tests/test_stream_batch.c` (0 of 266,240 floats differ at B=8, both dtypes) + `make test-stream-batch-allocs` (0 allocations/step); M1 dev signal 693.60 -> 227.00 ms per step at B=8
- [x] S1-5 The pool spins before it parks, with the meter: workers watch an atomic generation for `MYNAH_ASR_POOL_SPIN_US` (default 50, `0` = the pure condvar pool and the A/B arm) before sleeping, so the dozens of small GEMMs a stream step issues back to back stop paying a kernel round trip each. M1 dev signal, the sub-2M-MAC shapes: T=4 318 -> 219 us, T=8 355 -> 285 us; the large shapes do not move. `worker_spin_pct`/`caller_spin_pct` in the SIGUSR1 dump say whether the spin caught anything. **The A/B arm earned its keep immediately**: at `SPIN_US=0` the first dispatch DEADLOCKED (workers parked on the job condvar, caller on the completion condvar, 0% cpu) because a worker started from the live generation and could park on a job it was already counted in — invisible at the default spin, which catches the bump inside its window. `tests/test_threads.c` now gates the first dispatch at three spin budgets in pristine children, and the prefork `threadpool_after_fork` contract in a child of a warm pool. The lane redirect is not lifted: this runtime has no second lane to redirect → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [x] S1-6 BLAS leaves the worker: `src/sgemm.c` is ours, deterministic across thread counts and gated (`tests/test_sgemm.c`), behind one seam, and **`BLAS=none` is the Linux default since 2026-09-19**. What unblocked the decision was finding why `own` had lost: the DOT family — every `x @ W^T`, so every f32 linear, the joint head and the attention scores — ran one dot product at a time into ONE accumulator, i.e. FMA-latency bound at 8.7 GF/s on a core that does ~100. Register-tiled (`SG_DOT_MR x SG_DOT_NC_TILE`, a `1 x SG_DOT_NC_WIDE` strip for a gemv) it does 75-80, the panel shapes 48.9 -> 74.8 (`SG_PANEL_NV` derived instead of chosen), the LSTM gemv 8.2 -> 30.2; at 4-16 stacked rows — the streaming shapes — `own` now BEATS Accelerate's AMX. The tile is a schedule, not a formula: byte-identical to the untiled dot, gated for DOT/PANEL/NARROW by `tiling_identity` in `tests/test_sgemm.c`. The same instrument found a second defect: the parallel threshold was compared against total work rather than work PER THREAD, so the sub-2M-MAC shapes — where the streaming attention lives — cost 630 us at 8 threads against 318 at one; now 330. OpenBLAS keeps building as the comparison arm → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [ ] S1-6a Linux only, now a VERIFICATION rather than an open question, and two commands rather than a box-day: `MYNAH_ASR_GEMM_PROFILE=1` dumps the shapes and call counts the model really issued, `tests/bench_gemm_shapes.c` replays them against both arms interleaved in ONE process and weights each shape by its count. Run it on the Axion and on x86 with `make BLAS=openblas`; if OpenBLAS wins by enough to cost capacity the Makefile default goes back and the note says why. Also owed there: thread count inside a pinned prefork worker (2T vs T) and the OpenBLAS f32 row-stability gate that decides `MYNAH_ASR_BATCH_F32` → [`.work/threadpool-and-lane.md`](.work/threadpool-and-lane.md)
- [x] S1-7 Share the rel-pos K-projection across the streams of one batched pass that have the same `K`: computed once per (layer, K-group) into the batch scratch, bit-exact by construction and gated as such (0 of 266,240 floats differ at B=8, int8 and f32, with SHARED/PRIVATE/GROUP counters proving it happened); the single path is untouched and one allocation-free step is unchanged. M1 dev signal UNDER THIRD-PARTY LOAD, best-of-4 interleaved: slope `b` 22.7 -> 20.3 ms (-10.7%), B_max ~8.2 -> ~9.2 — a third of the 7.4 ms the isolated micro-benchmark had promised, discrepancy not explained. Only the largest K-group of a pass is shared; Linux unmeasured → [`.work/stream-api-v2.md`](.work/stream-api-v2.md)
- [~] S1-8 The S1-4 gates on Linux: **done for `own`** on the Axion (Neoverse-V2) — `tests/test_stream_batch.c` on the converted Nemotron pack is IDENTICAL OK at B=1,2,3,4,8 for int8 AND f32, the f32 encoder bit-exact at B=8 (266,240 floats compared, 0 differ, 0/24 caches differ), with the counters proving the fast path ran (208,896 `neon-smmla` kernel blocks, rel-pos shared 2496 / private 0). It also reported the pre-existing es-ES single-vs-offline int8 difference and attributed it correctly instead of letting it discredit the batching gate. STILL OPEN: the OpenBLAS arm (`MYNAH_ASR_BATCH_F32=1` with `make BLAS=openblas`, which decides that default there — the package is not installed on the box) and the step table → [`.work/axion-session-2026-09-19.md`](.work/axion-session-2026-09-19.md)
- [ ] S1-9 The es-ES clip transcribes differently offline and single-stream at int8, on both hosts, and nobody has decided whether that is a quantisation edge or a defect. The batching gates are unaffected and say so; this is the separate question → [`.work/axion-session-2026-09-19.md`](.work/axion-session-2026-09-19.md)

### S2 — The server
- [~] S2-1 Prefork parent, core-major pinned workers, SCM_RIGHTS handoff, `--prefork-plan` (landed; Linux pinned run pending) → [`.work/server-prefork.md`](.work/server-prefork.md)
- [~] S2-2 One scheduler thread per worker owns the model; slots; ingest threads; one path for WS and REST (landed and gated on the M1 dev host: identity at 4 and at 8 streams and under `--prefork`, ubsan clean, the step is one batched call; TSan and the Linux cadence numbers are what is left) → [`.work/server-scheduler.md`](.work/server-scheduler.md)
- [x] S2-2b The scheduler's step is ONE `mynah_asr_stream_step_batch` over the ready set, not one feed per slot: cancellation, fairness, per-slot lag and the finalize path unchanged, scratch reserved at start-up, `batched_steps_total`/`rows_stacked_total`/ready-set/`step_wall_ms` in `/v1/health` and `/metrics`. Quiet-host M1 pair, same gate back to back: 4 streams lag p50/p95 **1219/2193 -> 145/253 ms** (NOT STREAMABLE -> GOOD), WAVE 8x2 **1104/1952 -> 143/258 ms**; new `batched-identity` phase (8 streams, 5 clips, `--cap 8`) byte-identical and proving the stacked path ran → [`.work/server-scheduler.md`](.work/server-scheduler.md)
- [~] S2-3 Admission ladder with per-reason statuses, timeouts, pings, lingering close, graceful shutdown (landed and gated on the M1: every worker refusal goes through the lingering close, `--ping-ms`/`--max-audio-seconds`/idle caps announced with a code, SIGTERM drains with `shutting_down`, exit 0, `leaks --atExit` 0 leaks; the "lag does not move under a cap+1 refusal" line is served by the stalled-reader phase of the stream gate, not measured directly; rung 4 still not enforced at a step boundary; no Linux run) → [`.work/server-admission.md`](.work/server-admission.md)
- [x] S2-4 Asynchronous bounded output writer; backpressure is cancel (module landed with a model-free self-test, TSan and leaks clean; the scheduler writes every frame through it) → [`.work/server-stream-out.md`](.work/server-stream-out.md)
- [~] S2-5 WebSocket protocol v2: control messages, `seq`/`lag_ms`, refusals before the upgrade (query validated before the 101, `unknown_control` and `language_not_served` survive the session, `format=f32le`, `rate=` refused rather than resampled per frame, three utterances on one socket byte-identical to the CLI in `tests/test_server_protocol.sh`; the v1 aliases on `delta`/`done` and `t0`/`t1` are the last step) → [`.work/ws-protocol-v2.md`](.work/ws-protocol-v2.md)
- [~] S2-6 Several models in one fleet: `--model name=dir[:workers=][:cpus=][:cap=][:quant=][:lookahead=]` + `--default`, router by model (WS `?model=`, REST query or multipart field, inside the existing bounded `MSG_PEEK`), `model_not_found` 404 with the accepted set, per-group admission, `/v1/models` from the actual table, `group`/`groups` in `/v1/health`, a `model` label on the router's per-worker series (landed and gated on the M1: `tests/test_server_models.sh` runs ONE server with two groups under `--prefork 2` and proves each group's REST answer byte-identical to THAT model's CLI, the WS identity on the streaming group, 400 `model_not_streaming` on the offline one, 404 on an unknown name, capacity refused per group while the other still serves, survivors=0. NOT done: no Linux/pinned run, so `:cpus=` is a printed plan and not measured isolation; no Canary/AED group and no `--lid-model` in a multi-group fleet; no multi-group SOAK) → [`.work/multi-model-serving.md`](.work/multi-model-serving.md)
- [x] S2-7 Byte-identity gate: N concurrent streams == the same streams alone, also under `--prefork` (`tests/test_server_stream.sh`: 4 real-time streams x 2 utterances and `--prefork 2 --cap 2`, each text byte-identical to `mynah-asr transcribe` of the same clip; `tests/test_server_protocol.sh` adds three utterances on ONE socket) → [`.work/server-scheduler.md`](.work/server-scheduler.md)

### S3 — The server proves what it runs → [`.work/observability.md`](.work/observability.md)
- [x] S3-1 Flag registry + `[FLAGS]`/`[EFFECTIVE-CONFIG]` in library, CLI and server, `[SERVER-CONFIG] v=1` and a per-worker `[TOPOLOGY] v=1` with the mask read back from the kernel
- [x] S3-2 `--dispatch-map [--json]` resolved from owner predicates, `IDLE HARDWARE` footer, ISA guard first in both `main()`s; `mynah-asr-server --dispatch-map` prints the same table
- [x] S3-3 `/v1/health` as facts (cancels by reason, lag p50/p95, resolved kernels, actual mask); `/metrics` on its own port, token bucket, per-worker series, exact threshold counters, the router answering for the fleet
- [x] S3-5 `MYNAH_ASR_GEMM_PROFILE=1`: every call through the f32 seam recorded per distinct shape and dumped at exit (`[GEMM-PROFILE]`/`[GEMM-SHAPE]`), so "which shapes does this model issue, and how often" is a fact per family, per lookahead, per quantisation instead of a reading of the source → [`tests/bench_gemm_shapes.c`](tests/bench_gemm_shapes.c)
- [x] S3-4 `SIGUSR1` one-shot `[DUMP]` bracketed per worker, forwarded by the parent, handler installed before the fork; thread names verified and documented

### S8 — Predict and advise, not just measure → [`.work/where-to-attack.md`](.work/where-to-attack.md)
- [x] S8-0 The serving default is `int8`, not f32: measured 2.24x on one stream (M1, f32 RTF 1.528 vs int8 0.682 — f32 did not sustain ONE real-time stream), against a documented mean CER 0.133 -> 0.145 over 34 locales. Every capacity number in this repo was taken at int8, so the shipped default disagreed with the measurements. The CLI keeps f32, where it is the reference the other modes are checked against
- [x] S8-1 **Calibrate, do not assume**: `tools/bench/box_advisor.py` runs the REAL step table (`tests/test_stream_batch.c`, `MYNAH_ASR_STEP_TIME=1`) on the machine in front of it and fits `a` and `b`, discarding B=1 as warm-up. The sibling TTS doctor has to calibrate a proxy kernel because its frame cost is a composition; this runtime does not, so the coefficients are a fit of the SERVING PATH itself. `--no-calibrate` carries a constant in from another box and the tool then REFUSES to recommend when the two machines differ in cpu count by more than 1.5x — both `a` and `b` are functions of how many cores walk the weights, and carrying the pair across a 4x difference describes neither machine. Original wording:: the advisor runs the step table at B=1,2,4,8 on the actual box and fits `a` and `b` (discarding B=1 as warm-up), because a prediction from a spec sheet is a horoscope. ~30 s
- [x] S8-2 Predict per lookahead with the arithmetic shown and every figure labelled `[MEASURED]`/`[CALIBRATED]`/`[EXTRAPOLATED]`/`[PREDICTED]`/`[UNKNOWN]` — the fallback ladder degrades the LABEL, not the number, and `predict()` returns None rather than inventing a coefficient. Anything past B=8 is marked EXTRAPOLATED because that is where the step table ends. Per-quant is still open. Original wording: with the arithmetic shown and labelled a prediction: `B_max = (rho*P - a) / b` per worker, `W * B_max` for the fleet. Lookahead is the biggest single lever — 3 -> 6 nearly doubles capacity for 240 ms of latency — and belongs in the output as an explicit trade
- [x] S8-3 A ready-to-paste command line with W x T (wide first, and the reason printed), `--cap` at 90% of the prediction so the ladder refuses before the cadence does, `--quant int8`, and the lookahead that meets a stated `--latency-budget-ms`. It refuses on an UNKNOWN dispatch row, on a loaded box, and when the only cost model available came from a different-sized machine. Original wording:: W x T (starting WIDE, see S8-5), `--cap`, `--quant`, the lookahead that meets a stated latency budget, and the backlog/descriptor limits S7 already derives. Refuse when an input is missing rather than substituting a default
- [x] S8-4 Close the loop: **predicted 45, measured 16** on 24 Neoverse-V2 cores (2026-09-20), and the gap is now attributed rather than noted. `mynah_asr_step_b_wall_ms_sum`/`_count` fit `T_step(B)` from the step AS THE SCHEDULER CALLS IT: `a = 39.5 ms, b = 8.57 ms` at c=16 against the bench's `30.8 / 4.58`, and `b` degrades to 10.13 at c=24. Feeding those back gives `B_max = 25` against a ceiling of 16 with 20 breaking — prediction and measurement in the same neighbourhood for the first time. The bench `b` is optimistic by 1.8x because the bench step does not share the machine with ingest, writers, delta JSON and sockets → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md) §7
- [x] S8-5 Both predictions tested on 2026-09-20 and the answers went opposite ways. (a) **`a` is NOT weight traffic**: it went 84.1 → 30.8 ms from 8 to 24 cores, which is compute scaling, not a memory wall — so A5 (bf16, weight layout, prefetch) loses its main justification and must be re-argued before anyone spends a week on it. (b) **FEW WIDE workers is right**, but only after the gate was fixed: as first scored, 8x3 looked twice as good as 1x24, and every winning rung was losing streams that the harness called MARGINAL. Re-scored with losses counted, 1x24 holds c=16 with zero losses and the narrow shapes hold c=8. A third prediction fell out of the same run and is more useful than either: **widening the batch does not move the ceiling** (mean ready set 3.69 → 7.98 for no gain), so the FIXED cost is not the limit — the per-stream term is → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md) §6, §9

### S6 — The fleet answers for itself → [`.work/fleet-observability.md`](.work/fleet-observability.md)
- [ ] S6-1 Fleet metrics through a shared slot table: one `mmap(MAP_SHARED)` before the fork, one fixed slot of atomics per worker, single writer, relaxed adds; the router renders every worker's SCHEDULER counters with a `worker` label beside the routing ones it already has. One scrape target, per-worker series preserved, nothing summed, no IPC in the step (the qwen-tts `qwen_metrics_slot_t` mechanism). Today the router exports routing facts only and the real counters are reachable only by giving each worker its own `--metrics-port`
- [ ] S6-2 Fleet `/v1/health` on the router, and `worker`/`counters_scope` disclosure on the worker's own: a probe currently lands on whichever worker the router picked, so it is a per-worker health wearing the server's name. Readiness and liveness stay ONE endpoint — only a server that HAS a scheduler can be unavailable for want of one
- [ ] S6-3 `configs/observability/`: `prometheus.yml`, `grafana-dashboard.json`, `alerts.yml`, README, and an `observability_up.sh` that provisions both on loopback. Panels translate one-for-one from the sibling once the vocabulary is ASR's; every alert fires on something a LISTENER would notice and every threshold is an exact count, never an estimated quantile
- [ ] S6-4 **Backlog**, the one counter this server is missing: audio received and not yet encoded, as exact threshold counters (`backlog_over_chunks_total`), not a gauge. It is the ASR mirror of the sibling's behind-realtime gap and the one server-side number that says a worker is falling behind before a client notices
- [~] S6-5 Quality, as opposed to speed: **landed** — every utterance gets a CER against the bank's human reference (`--transcripts`), the normaliser is defined once in the metrics module because a CER computed with a different one is a different number wearing the same name, and a run whose cadence held while the transcripts got worse is now **DEGRADED** rather than GOOD. TTFB (first frame of any kind) is reported beside TTFP (first word): together they say whether a wait is transport or model. Known-answer tests pin the normaliser, the edit distance, the rate and the verdict. STILL OPEN: a CER baseline (the `cer_p95` threshold is a placeholder guarding against collapse, not a quality target), and the CER drift track across commits. The load harness must REFUSE to print a KPI table when the batched-identity comparison fails (the sibling's `serve_identity_gate.py` rule — a number from a run whose transcripts moved is worse than no number); a CER drift track against the committed `samples/` bank; and named drift thresholds in the soak the way the sibling names its 30%/20%/resource-growth gates
- [ ] S6-6 A metrics-page watcher under `tools/` (not written yet): scrape the page under load and assert monotonic (no `_total` went backwards), conserved (`completed <= assigned`), balanced (every live worker received something). Model-free, and the only thing that exercises the metrics port under load

### S7 — C100 preconditions (code, before the box) → [`.work/fleet-observability.md`](.work/fleet-observability.md)
- [x] S7-1 The listen backlog is derived from the capacity the ladder already declares (`W x (slots + queue)`, doubled because the connections that will be REFUSED arrive in the same burst as the ones that will be served, floored at `SOMAXCONN`, capped at 4096, overridable with `MYNAH_ASR_LISTEN_BACKLOG`) and printed on `[SERVER-CONFIG]` beside the kernel `somaxconn`, with a WARNING when the kernel clamps it. The defect was reproduced before the fix: `listen(fd,64)` + 150 simultaneous connects completed 64; with the derived backlog it completes as many as `somaxconn` allows
- [x] S7-2 `RLIMIT_NOFILE` is raised toward the hard limit at start-up with a halving backoff and REPORTED AS IT BECAME, not as it was asked for; a loud WARNING names soft, hard and the need when the ceiling is below the configuration, because a stream refused by `accept()` with EMFILE is a refusal no rung of the ladder issued and no counter explains
- [ ] S7-3 The load generator is one PROCESS per stream, so at C100 it competes with the server for cores. The sibling MEASURED this on the same Axion: an unpinned generator cost worker 0 ~8% of first-audio latency. Either a second host generates the load, or the generator is pinned to a reserved slice excluded from the server's mask — and the harness records which, because a C100 number taken with the generator on the server's cores is not a C100 number

### S4 — Measure, then qualify → [`.work/bench-harness-streaming.md`](.work/bench-harness-streaming.md)
- [x] S4-1 Streaming metrics module: every metric defined once, known-answer self-test in `make test` ([`tools/bench/streaming_metrics.py`](tools/bench/streaming_metrics.py))
- [x] S4-2 WAVE / SOAK modes, mixed-length bank, manifest, drift gate, refusal on pacing failure ([`tools/bench/stream_load.py`](tools/bench/stream_load.py); exercised on the macOS dev host only — the qualifying run is S4-3)
- [ ] S4-3 First SOAK on the ARM box at the derived operating point (c=16, 1x24): the ladder screened it on 2026-09-20 and screening never promotes. It is what moves `configs/perf/axion-c4a-highcpu32-nemotron-streaming.json` from `screened` to `qualified` → [`.work/axion-first-run.md`](.work/axion-first-run.md)
- [x] S4-5 [`tools/bench/box_session.sh`](tools/bench/box_session.sh): doctor → dispatch map → calibration → WAVE ladder → SOAK → REST ladder as ONE budgeted command. It polls `/v1/health` and refuses with the server's own log when the process is not serving — the first Axion ladder ran five rungs against a server that had exited on an unsupported `--lookahead` flag and produced five well-formed artefacts containing nothing (`.work/axion-session-2026-09-20.md` §1). `sleep 25` is not a readiness check
- [x] S4-6 [`tools/bench/rest_load.py`](tools/bench/rest_load.py): the concurrency ladder for models the runtime refuses to stream. Reports xRT (throughput) beside per-request latency and names the knee without choosing it; a 503 is counted as admission working, a non-503 invalidates the rung
- [x] S4-8 [`configs/perf/`](configs/perf/README.md) + [`tools/perf_profile.py`](tools/perf_profile.py): one JSON per box shape and model — topology, settings, the dispatch rows the binary MUST resolve, the gates, and what was measured with its date. `check` is a preflight that refuses when the world disagrees; `validate` refuses a profile that claims `qualified` with no soak behind it; every environment variable carries a `value` and a `why`, and `value: null` means it must be ABSENT. Written because three runs on 2026-09-20 were invalid in ways their numbers did not show, and a configuration that lives in shell history cannot be compared with itself a week later
- [x] S4-9 A lost stream disqualifies a rung. `reader: timed out` was scored MARGINAL beside the percentiles; it is a session the caller lost, and it made a W x T sweep read backwards. A 503 is deliberately still MARGINAL: refused at the door and dropped mid-sentence are different outcomes
- [ ] S4-7 Run the stress bank (`make fetch-stress-bank`) and soak against it instead of the five committed clips: the bank manifest has never been produced, so every soak so far has had a four-clip corpus
- [x] S4-4 Serving operations doc: how a new box is approached, the vocabulary, the envelope → [`docs/serving.md`](docs/serving.md)

### S5 — Kernels for the batched step, ARM and x86 in one step → [`.work/cpu-kernels-arm-x86.md`](.work/cpu-kernels-arm-x86.md)
- [x] S5-1 Weight-stationary int8 GEMM for T <= 64, bit-exact vs single rows: SMMLA (2x2 tile), SDOT x4, EVEX and VEX VPDPBUSD, AVX2 — all behind target attributes in every build, chosen by a runtime probe, opt-out through `MYNAH_ASR_CAPS`. `--dispatch-map` splits `kernel.int8_dot` (one row) from `kernel.int8_rows` (the batched pass). It caught a real defect on the way (the `-ffast-math` epilogue grouping `s*ws*sx` differently at two inline sites, now pinned by a value barrier). **CLOSED 2026-09-19 on the Axion (Neoverse-V2, i8mm present)**: `simd=neon+dotprod+i8mm`, `kernel.int8_rows` resolves to `neon-smmla`, and `tests/test_qmat.c` reports `neon-smmla exact OK, 82 shape/T cases, 466 kernel blocks` — the kernel EXECUTES and is bit-exact, which is what `[~]` was waiting for. The x86 arms are still cross-compiled only and skip with a reason → [`.work/cpu-kernels-arm-x86.md`](.work/cpu-kernels-arm-x86.md)
- [ ] S5-2 Depthwise conv with transposed weights and SIMD FMA, gated by the profile
- [ ] S5-3 KleidiAI as an A/B arm behind `KLEIDI=1`
- [ ] S5-4 `SIMD=` profiles with the double test, build-flag stamp, link-only CI per ISA
- [ ] S5-5 Pool-meter run on the batched step; choose W×T from the break-even rule

### Hygiene → [`.work/repo-hygiene.md`](.work/repo-hygiene.md)
- [x] H-1 the per-language samples manifest under the test audio is generated by `make fetch-lang-samples`: say so or track it
- [x] H-2 Stale test-file reference in `tools/gen_kquant_ref.py`
- [x] H-3 `make check` (plan + repo + flag registry) green locally and a CI step (runs on the next push)

### Multi-model — the fleet beyond Nemotron → [`.work/multi-model-streaming.md`](.work/multi-model-streaming.md)
- [x] M-1 A model that declares streaming presets but uses something the incremental encoder does not implement (linear biases, a folded batch_norm, xscaling, symmetric conv padding, per-feature mel normalisation, a subsampling factor other than 8) is REFUSED with a named reason instead of emitting plausible wrong text: `mynah_asr_stream_unsupported` asks the loaded weights, the server asks it before the WebSocket upgrade (`400 model_not_streaming`, the reason in the body). No shipped pack trips it — that is the point
- [x] M-2 The model constants that were not: the subsampling factor is now DERIVED from the convolutions `src/subsampling.c` applies (`MYNAH_ASR_SS_STAGES`/`MYNAH_ASR_SS_FACTOR`) and a pack whose `encoder.subsampling_factor` disagrees is REFUSED at load rather than silently mis-timestamping every word; the CTC blank comes from `decoder.blank_id` (a pure-CTC pack declares it, a hybrid's auxiliary head keeps the last index because the pack's `blank_id` belongs to its RNNT/TDT decoder); the VAD reads its own `sample_rate`; and the server asks `mynah_asr_sample_rate()` instead of dividing by 16000 in six places — the `rate=` refusal now names the model's rate. No behaviour changes on any shipped pack (the converter writes `blank_id = vocab_size - 1`, every pack is 16 kHz and subsamples by 8): the point is that each of these was true by coincidence
- [ ] M-3 Converter builder for `nemotron-speech-streaming-en-0.6b` (cache-aware, left context 70): the only cache-aware model not served, and for a converter reason rather than an architectural one
- [ ] M-4 Buffered streaming for Parakeet behind an explicit stream mode, in its own group. **Confirmed at runtime, not only by arithmetic**: `mynah_asr_stream_unsupported()` refuses every converted Parakeet pack on three counts (linear biases, folded batch_norm, symmetric conv padding), so the server declines the upgrade with a name and Parakeet has no streaming concurrency to measure — only offline throughput (S4-5). Buffering costs ~7x real time per stream against ~1.05x for cache-aware Nemotron; the cap comes from a measured step, and `docs/serving.md` states the multiplier before a line is written
- [-] M-5 Canary streaming: out of v2 by design — its AED decoder keeps no state between calls, so it re-decodes the window on top of the 7x encoder

### S9 — The encoder path under the scheduler → [`.work/axion-session-2026-09-20.md`](.work/axion-session-2026-09-20.md)
- [x] S9-1 A group of one goes through the stacked path. `B == 1` returned to the single path "verbatim" and `if (batched && g > 1)` sent a lone preset group there too, so a chunk ran as a chain of `[1, d]` GEMVs instead of one `[R, d]` GEMM: **86.7 ms against 19.2 ms per stream**, and two streams together cost less than one alone. The step table called that row warm-up for days; it was not. Rule 4 holds (IDENTICAL, `dot_rows 0 → 3840`), and the identity gate — which compared B=1 against the single path while B=1 WAS the single path — stops being vacuous
- [ ] S9-2 Measure S9-1 on the box: step table and the same ladder as the 2026-09-20 baseline (c=16 held, c=20 broke 3/3). The prediction is that low concurrency improves most and the ceiling moves some; if the ceiling does not move at all, the per-stream term is elsewhere
- [ ] S9-3 Why does `b` grow with concurrency (8.57 at c=16, 10.13 at c=24)? A per-stream cost that degrades under load is the thing that actually sets this ceiling, and nothing in the cost model predicts it. Candidates, in order: cache pressure from B live KV caches, the decoder's f32 pred-net per emitted token (A7), and the per-delta JSON on the scheduler thread
- [ ] S9-4 The stall: every stream gets a first partial, then nothing for tens of seconds, then partial recovery. Seen twice on used servers, never on five clean repeats, and **every prefork topology in the W x T sweep lost streams to it while 1x24 lost none**. `box_session.sh` now samples `steps_total` through a rung, so the next occurrence says whether the server stopped stepping
- [ ] S9-5 Take the offline REST work and the finalize tails off the scheduler thread, or ration them with a wall-clock budget per step: `sched_run_jobs()` stalls every stream on the worker for its duration and the code says so in a comment

### S10 — After the audit of 2026-09-20: the hot path beyond F27, the harness, quality, the tiers → [`.work/serving-audit-metrics-tests-hotpath.md`](.work/serving-audit-metrics-tests-hotpath.md)
- [x] S10-1 One rel-pos table per layer at load, `RK = pe(Kmax) @ relk_w^T`, indexed by `(Kmax-K)` and sized K+Q-1 rows (half of every projected row is never read by the rel_shift). Removes `relpos_priv`, `relpos`, `pos_emb` per step and the S1-7 group machinery; bit-identical by the DOT-family row independence, gated by `tests/test_stream_batch.c`; **LANDED 2026-09-21** (`5fc9d36`): 0 of 266,240 floats differ at B=8, int8 and f32, with the table built BETWEEN the single and batched passes so the comparison is table-vs-per-step; the position embedding is no longer built at all; `MYNAH_ASR_RELPOS_TABLE=0` restores S1-7 and is equally exact. Dev host `b` 18.52 → 14.06 ms/stream (1.32x, Darwin, DIAGNOSTIC). The knee HAS now been remeasured on the Axion 2026-09-21 → **F29**: emission lag p95 at C=40 616 → 229 → 95 ms across arms A/B/C, first breaking concurrency 40 → 70, knee 65 confirmed, `T(B) = 4.02 + 11.84·B` (R²=1.00000) over B 1.36–17.04 against the frozen `14.52 + 19.00·B`
- [x] S10-2 RNNT joint head off the small-T serial dot path: `mynah_asr_qmat_mul_rows` (bit-identical by S1-4), then stacked across the B streams of a step so the 8.4 MB head is read once per step, not once per frame per stream — **LANDED 2026-09-21** (`ebd91de`), causally screened on the box in F29: arm B (this change plus the dead-row narrowing, rel-pos table OFF) holds C=40 where arm A breaks, emission lag p95 616 → 229 ms. The two effects cannot be separated further by this experiment and the entry says so
- [ ] S10-3 The elementwise stages the scheduler thread runs alone while the pool idles: `parallel_for` over streams for attention, conv, SiLU, subsampling; activation quantisation by row (the 2^21 threshold is never met); SiLU fused into the qgemm epilogue
- [ ] S10-4 Pool barriers per step counted and the park/spin meter read at C=32, then `MYNAH_ASR_POOL_SPIN_US` swept in SOAK (the sibling: neutral in wave, +11 % p95 in soak); one persistent region per step with spin barriers is the structural answer if the count is the cost
- [ ] S10-5 K/V cache written in place (`[left+Qmax, d]`, compaction every left/Q steps) instead of a 600 KB copy plus a 540 KB shift per layer per stream
- [ ] S10-6 Subsampling stacked across B and its 17.8 MB f32 linear quantised; `xn` quantised once for q/k/v; depthwise weights transposed at load
- [ ] S10-7 VERIFY on the box that `expf` is vectorised in the Linux binary (`objdump` for `_ZGVnN4v_expf`); if scalar, a branch-free sigmoid with the golden gate
- [ ] S10-8 Layer norm and mel front-end out of double: f32 two-pass norm, twiddle table and real FFT, each behind its oracle gate
- [ ] S10-9 The delta framing allocates ~30 times per delta on the scheduler thread and no gate covers it: an arena for cJSON or hand-formatted frames, and a server-side allocation gate beside the two library ones
- [x] Q-1 One CER and one percentile: `test_langs.py` imports `streaming_metrics` (NFKC and full Unicode punctuation move there), `rest_load.py` uses the same `pct`; `load_transcripts` keyed by `lang/file` (today 11 languages collide on `fleurs_1521.wav` and score against the wrong reference) — **LANDED 2026-09-21** (`a822d31`): the normaliser is NFKC + Unicode punctuation, `test_langs` and `rest_load` import it, and the fractional-rank bug it exposed (p99.9 of 1000 samples WAS the maximum) is fixed and pinned
- [~] Q-2 **Observational half DONE 2026-09-21** (`tools/eval/rnnt_earliness.py`, 21 utterances, N-sensitivity reported): correct early evidence exists in 19–62 % of utterances depending on the stability rule, always quantised to one chunk, but the wrong-runner-up fraction RISES with stricter stability (19→25→33 %). Still owed before R-5: first-correct-lexical-token latency, partial revisions, false non-blanks in leading silence. **Blocks R-5** (R-3F): the baseline must also detect a premature or wrong FIRST token, partial instability, and hallucination in leading silence — today it covers final CER only. Committed CER baselines per model, quant and lookahead under `configs/quality/`, and a regression gate with a margin in `test_samples.py` and `stream_load.py --transcripts`; `perf_profile.py validate` refuses `qualified` without one
- [ ] Q-3 Streaming-vs-offline CER delta per lookahead over `samples/`, and CER on REST (`rest_load --reference/--transcripts`), so Parakeet and Nemotron are scored by one report on one corpus
- [ ] Q-4 Dead and duplicate tooling: `tests/test_kquant.sh` (it execs a `test_gguf` binary that has no source and no make rule, so it has only ever exited 77), the tracked `tests/test_ingot_parity` binary, the 94.6 s clip in the `bench-stream-wave` default, five WebSocket client copies, two box orchestrators outside `make`, `perf_profile.py validate` outside `make check`
- [x] T-1 Reporting-only additions to `streaming_metrics.py` from data the JSON already holds: p99/p99.9, audio-seconds per wall-second, end-of-utterance latency, per-stream fairness and max inter-delta gap, WER, bootstrap CI; `getrusage` + `/proc/self/status` in `[DUMP]`; a cross-run comparator that refuses unequal manifests — **LANDED 2026-09-21**: `tools/bench/compare_runs.py`, p99/p99.9 withheld below the n that can name a rank under the maximum, fairness as worst-over-median, longest inter-delta gap, audio per wall second, end-of-utterance latency, WER
- [x] T-2 Tier 0 (~90 s, one command per model): dispatch map, self-tests, one warm server, 4 WS streams with CLI reference and `samples/` CER, 4 REST, dump, zero survivors. Tier 1 (~8 min): knee by bisection on a warm server with validity-length rungs, cold quality pass, REST rungs for the offline model. Tier 2 (~30 min): two identical 600 s soaks at the profile's C with the stress bank and `--transcripts`, `perf_profile.py promote` — **LANDED 2026-09-21**: `tools/bench/tier.sh` + `make tier0/tier1/tier2`, with `tools/bench/knee.py` (bisection on one warm server), `tools/eval/cer_offline.py` (baseline + margin) and `compare_runs.py`; tier 2 refuses a dirty tree and refuses to run without a reference manifest
- [ ] T-3 `make fetch-stress-bank` run once and every soak pointed at it (S4-7): until then no soak scores a transcript and no operating point can be promoted

### R — First-partial responsiveness: the other SLO → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)

Opened 2026-09-21 from F29: the steady state moved by nearly a factor of two
while the wait before the first word did not move at all. The goal is not to
lower 2.6 s, it is to account for all of it. No optimisation before the C=1
waterfall reconciles.

- [x] R-1 Prove from code what every timestamp on the path means and in which clock domain, `stream_open` through `client receives first partial`; name `TTFP-open`, `TTFP-audio` and `TTFP-speech` separately and instrument speech onset rather than substituting stream open for it — **DONE 2026-09-21**: today's `ttfp_ms` is relative to the completion of the first audio WRITE, so it is neither open- nor speech-relative; the harness and the server were on two different clocks on macOS (`mach_absolute_time` vs `CLOCK_MONOTONIC`, 4.63 days apart, a no-op on Linux) and the harness now reads `CLOCK_MONOTONIC`; the server publishes only three instants between audio-in and text-out, so `publication_delay` cannot be computed today; `s->t_first_delta` is written and read by nothing; the batch window, the scheduler tick and Nagle are all cleared as suspects; the first chunk needs 256 ms of audio, not 320
- [x] R-2 One stream, C=1, a millisecond waterfall that reconciles with the measured first-partial latency; report `audio_seconds_consumed_at_first_nonblank` and `wall_time_of_first_nonblank` explicitly. A concurrency effect must not be allowed to hide a fixed pipeline floor — **DONE 2026-09-21** (`MYNAH_ASR_TRACE_TTFP`, `tools/bench/ttfp_waterfall.py`, eight clips, onsets 0.15–3.45 s): **branch B**. `publication_delay` is 0.12–0.23 ms, so the serving path owns none of the wait; the RNNT consumes **0.50–0.97 s of speech** and 2–13 blank steps before its first non-blank, and in 7 of 8 `TTFP-open` is that audio plus one step wall. The pipeline idles 270–291 ms between 30–46 ms steps against a 320 ms period: waiting for audio, not starved. A fresh server's FIRST step costs 1236 ms and blocks two chunks behind it
- [x] R-3 Classify every component of that waterfall as MODEL ARCHITECTURE / DECODING POLICY / OUR IMPLEMENTATION / REAL-TIME INPUT / COLD-START / BENCHMARK ARTIFACT / UNKNOWN. No optimisation proposal is admissible without this classification — **DONE 2026-09-21** (`MYNAH_ASR_TRACE_RNNT`): there is no decoding policy to blame — the greedy is a plain argmax with no threshold, temperature, suppression or commit rule, and the one gate we owned (`total > chars_emitted`, which drops a language-tag-only step) fired 0 times in 3 clips. The first frames are STRONGLY blank (margin ~+100 in silence, +4..+9 after onset) and only the crossing is near-boundary (−0.45..−2.61). Preset `[56,3]` puts 240 ms of right context inside each decision, so the token cannot exist earlier and is then published in 0.2 ms. The wait tracks SPEECH (+0.46/+0.56/+0.79 s) and not silence (0.18→3.45 s, a 19× range). Cold start is UNKNOWN and not reproducible (43.8/49.4/1010.6 ms over three fresh processes)
- [x] R-4 `publication_delay = client_first_partial − model_first_hypothesis`, instrumented as a first-class metric: it is the discriminant that says whether the serving path or the model's own emission policy owns the seconds — **DONE 2026-09-21** → F30: **0.08–0.10 ms** on the Axion, 0.12–0.23 ms on the dev host, at every concurrency from 1 to the knee. The serving output path owns a tenth of a millisecond of the first word and is not a candidate for optimisation
- [~] R-5 **Family B screened and STOPPED at phase 1, no VM started** (2026-09-21): `[56,0]` is the only preset below 3; proved from code that it takes the cadence period 320→80 ms and the first chunk 256→16 ms. It buys a median 100 ms of speech before the first non-blank and costs mean CER 0.0405→0.0525 (8 clips worse, 4 better), so quality disqualifies it before the capacity tax is counted. The hybrid fast-start→normal-cadence idea is recorded as an UNPROVEN hypothesis: a lookahead change closes and reopens the stream today, and a mid-stream switch has no gate. Original scope, family B only (context/cadence). Q-2 ruled out family A: a stably WRONG runner-up is exactly as early (median headroom 300 ms, p95 700 ms) and exactly as stable as a correct one, the margin trajectories are indistinguishable, and one stable runner-up in four is wrong. Smallest reversible A/B on the dominant term only, `before → mechanism → after`, same audio and build but the switch, transcript gate green; never trade recognition quality, transcript semantics, established streams or steady-state capacity for it
- [~] R-6 Load dependence: the DIAGNOSTIC half is done → F30 (TTFP-open p95 1535→1719 ms from C=1 to the knee while emission lag p95 goes 17→268 ms; speech needed before the first non-blank is 0.716 s at C=8/32/65 alike). What remains is the synchronised-opening A/B, which is the only thing that would upgrade F29's TTFP reading from inference to fact. Original scope: C1 → C8 → C32 → near-knee, then synchronised opening on its own, reporting TTFP-speech p50/p95/p99, TTFP-open p50/p95, model first-hypothesis latency, publication delay, emission lag, backlog, loss, CER and audio/wall together, so responsiveness cannot be bought with capacity

- [ ] R-7 **OPEN / UNPROVEN, not started.** Mid-stream lookahead transition: pay the fast cadence only until the first useful partial, then return to `[56,3]`. Today a lookahead change closes and reopens the stream and loses the K/V and conv caches (`sched_ensure_stream`); dimensionally a downward switch would fit, but frames already cached were computed under a different attention window and nothing gates that. **The correctness gate is written before any code**: a stream whose `right` changes mid-utterance without reopening must produce a defensible transcript against both fixed-preset references, the caches must be shown to carry the same meaning across the switch, the scratch and rel-pos table must be proved large enough for both regimes from `mynah_asr_enc_stream_init`, and the dangerous direction (q=1 → q=4) must be covered. If the transcript does not hold at library level, R-7 closes there and no serving change is designed → [`.work/first-partial-responsiveness.md`](.work/first-partial-responsiveness.md)

### Later, not now
- [ ] L-2 Unstable-tail partials for Nemotron (a decoder change; protocol already has `final`)
- [ ] L-3 CUDA v2b resident encoder (from the v1 TODO; GPU serving is out of v2 scope)

## Durable contract (changes only when a decision changes)

1. Config-driven: every model number comes from the converted pack's
   `mynah.json`; no model constants in `#define`s.
2. One code path for offline and streaming, and for REST and WebSocket in the
   server: a file is a slot fed faster than real time.
3. Every numeric stage is validated against the Python oracle
   (`tools/oracle/`) before anything is built on it; per-stage tolerances,
   transcripts byte-identical.
4. A stream's transcript never depends on who it was batched with: every
   batched path ships with a bit-exactness gate against the single-item path.
5. CPU is the product. Linux x86-64 and ARM64 are production; macOS numbers are
   development signals. A backend claim names its gates (rule 4 of
   `ENGINEERING.md`).
6. Load never enters the chunk size. When the machine is full, refuse.
7. Python is offline tooling only (`uv`); never required at runtime.
8. Everything in the repo is English. Commit messages: imperative, what and why.
